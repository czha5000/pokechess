# 纹兽战记 · 项目工作指南(任何 agent 接手前先读这份)

> 本文件是整个项目(网页游戏本体 + UE5 移植实验 + AI 美术管线实验)的总导航。目的:新会话/新 agent 不用翻聊天记录、不用满目录乱找,看这一份就知道"这是什么项目、代码在哪、文档在哪、该读哪份"。

---

## 1. 项目是什么

**纹兽战记**:战棋 SRPG × Roguelike × 怪物收集培育。原型是纯前端 vanilla JS 网页游戏(无构建工具,双击 `index.html` 直接跑),目前在并行做两件延伸的事:
- **UE5 垂直切片移植**:把网页版的战棋玩法用 Blueprint 在 UE5 里重做一遍(教学/学习向,手把手节点级)。
- **2D→3D AI 美术管线实验**:概念图 → ComfyUI 本地生成 3D 灰模 → 绑骨套动画 → UE Sequencer 过场。

## 2. 文件夹地图

**⚠️ 重要**:`game/` 根目录本身很乱,里面混着大量**历史备份**(旧版 demo html、旧版 GDD、几十个 `.tar`/`.zip` 快照)。**当前项目的唯一真实源码/文档在子目录 `纹兽战记/` 里**,根目录那些带版本号/日期的文件都是备份,不代表现状,不要从那里读代码或文档。

```
game/                                  ← 用户选中的根文件夹,大量历史备份混杂在这层
├─ 纹兽战记/                           ← ✅ 唯一的当前项目目录,一切以这里为准
│  ├─ index.html, css/, js/            ← 网页游戏本体(见下方"网页游戏代码结构")
│  ├─ assets/                          ← 过场视频/音乐素材
│  ├─ balance/                         ← 无头平衡模拟台(sim.js)+ wenshou-balance 技能
│  ├─ autoplay-review/ , wenshou-autoplay-review/  ← ⚠️ 两个文件夹内容完全重复(见第5节)
│  ├─ wenshou-autoplay-review.skill    ← 打包好的技能安装包(.skill = zip)
│  ├─ CHANGELOG.md                     ← 网页游戏改动日志,**改动历史的唯一权威来源**
│  ├─ README.md                        ← 网页游戏代码结构说明
│  ├─ UE协作Harness规范.md              ← ✅ 做 UE 部分前必读,协作协议
│  ├─ UE蓝图状态.md / UE节点备忘录.md / UE测试用例.md  ← UE 工作三件套(见 Harness 规范)
│  ├─ UE实操教程.md                    ← UE 垂直切片总进度(续接区在顶部)
│  ├─ UE美术管线.md / UE过场动画管线.md ← 2D→3D 美术/动画管线手册
│  ├─ UE学习笔记.md / UE学习路线图_教练模式.md / UE开发可行性报告.md / UE移植课程大纲.md  ← 早期规划类文档
│  └─ 其余设计文档(见第4节)
├─ 纹兽战记_GDD_v0.1/v0.2.docx          ← 旧版 GDD,已被 纹兽战记/ 内的设计文档取代
├─ 纹兽战记_demo_v0.1~v0.8.html         ← 单文件时代的历史版本,已被 纹兽战记/index.html 取代
├─ 纹兽战记_snapshot_*.tar (约30个)     ← 手动备份快照,按时间戳命名
├─ 纹兽战记*.zip / .tar                 ← 更早期的整体打包备份
└─ _t1 / _t3 / .~lock.*.xlsx#           ← 空文件/Excel锁文件残留,疑似垃圾文件
```

**UE 工程**(另一个挂载文件夹,不在 `game/` 下):`C:\Users\AI_Work\Documents\Unreal Projects\MyProject 5.8`
- `Content/Maps/` —— 对应 `UE蓝图状态.md` 里 GUID 引用的 `/Game/Maps/BP_Unit` 这类路径。
- 这是标准 UE 工程目录(`.uproject`/`Config`/`Content`/`Saved`/...),蓝图是二进制 `.uasset`,不能直接读写,只能通过编辑器操作(见 `UE协作Harness规范.md`)。

## 3. 网页游戏代码结构(`纹兽战记/`)

依赖单向:`ui → core → data`。
- 改**数值/难度** → `js/data/config.js`(`ENEMY_POWER`/`BOSS_HP`/`DEF_K` 等)、`js/data/*.js`
- 改**战斗规则/手感** → `js/core/combat.js`(伤害/命中/暴击公式)、`js/core/turn.js`(行动序)
- 改**外观** → `css/style.css`、`js/ui/*.js`
- **验证数值改动** → `balance/sim.js` 无头模拟(`wenshou-balance` 技能)、`balance/difftest.js` 三实现一致性校验
- **每次改动记得写入** `CHANGELOG.md`(约定格式见文件头)

## 4. 任务类型 → 该读什么

| 你要做的事 | 先读 |
|---|---|
| 改网页游戏数值/平衡 | `CHANGELOG.md`(最近改了什么)→ `js/data/config.js` → 需要时用 `wenshou-balance`/`wenshou-autoplay-review` 技能验证 |
| 网页游戏设计/剧情/玩法规划 | `剧情大纲.md`、`深化规划.md`、`玩法扩充清单.md`、`流派表.md`、`遗物事件一览.md`、`loop工程_恶意与趣味指标.md`、`恶意设计方案.md` |
| 平衡/深度相关的分析报告 | `验收报告_v0.67.md`、`改动总结_v0.55-v0.71.md`、`研究综述_深度与趣味的衡量.md`、`平衡测试方案.md` |
| UE5 蓝图开发(核心) | **`UE协作Harness规范.md`(必读)** → `UE实操教程.md` 续接区 → `UE蓝图状态.md` → `UE节点备忘录.md` → `UE测试用例.md` → 当前操作清单 `ue/STEP5b_DEF_K操作清单.md` |
| UE 粘贴块生成 | `ue/tools/paste_gen.py`(Cloud 替代原 paste-gen skill)→ `ue/paste/` |
| UE 美术/动画管线(2D→3D、过场) | `UE美术管线.md`、`UE过场动画管线.md` |
| UE 早期规划/可行性(历史参考,非当前进度) | `UE学习笔记.md`、`UE学习路线图_教练模式.md`、`UE开发可行性报告.md`、`UE移植课程大纲.md` |
| AI 自动打局调平衡 | `wenshou-autoplay-review` 技能(依赖 `balance/` 平衡台) |

## 5. 已知冗余(部分已清理,2026-08-09)

- ✅ **已删除**(明确垃圾文件,已获用户确认):`_t1`、`_t3`(0 字节空文件)、`.~lock.纹兽战记_demo平衡表_v0.1.xlsx#`(Excel 锁文件残留)、`纹兽战记/ziINjn9k`(与 `wenshou-autoplay-review.skill` 内容重复的乱码文件名 zip)。
- ⏸️ **按用户决定暂不处理**,仍然存在,仅记录:
  1. `autoplay-review/SKILL.md` 与 `wenshou-autoplay-review/SKILL.md` 内容完全相同(diff 确认过),重复文件夹。
  2. `game/` 根目录的 ~30 个 `纹兽战记_snapshot_*.tar` + 4 个 `.zip` + 8 个旧版 demo html + 2 个旧版 GDD docx——`纹兽战记/.git` 只有 3 个 commit(不覆盖到 CHANGELOG 里的 v0.71),这些手动快照可能是唯一的完整备份历史,**不建议删除**,可考虑以后归档进子文件夹。

## 6. 协作总原则

- **先读文档,再问用户** —— 状态/进度/已知坑都尽量维护在文档里,减少每次都要用户重新说明现状的成本。
- **改了什么就更新对应文档**,尤其 `CHANGELOG.md`(网页游戏)和 `UE蓝图状态.md`(UE 蓝图)——这两份是"现状"的权威来源,过期了下一个 agent 就会被带偏。
- **UE 相关的协作细则**(粘贴块生成规则、验证口径、人机分工)全部在 `UE协作Harness规范.md`,不在本文件重复。
