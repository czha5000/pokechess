# 纹兽战记 · 协作须知(每次会话都会自动加载,别绕过)

## UE 相关工作,硬性前置动作

**做任何 UE5/蓝图相关的事之前,先读 `UE协作Harness规范.md` 并照做**——不要只凭对话历史/摘要判断现状,那些会被压缩、会丢细节。

规范的核心,这里再钉一遍(完整版看那份文件):

1. **接手/开始任何 UE 任务前,必读这两份**(共约 130KB,别再通读 542KB 全文):
   - **`UE硬规则.md`** —— 86 条踩坑的纯结论提炼,做任何事之前先扫一遍。
   - **`UE蓝图状态.md` 的 `## BP_*` / `## WBP_*` / `## 系统` 快照章节** —— 每个蓝图现在有什么变量/函数/怎么连的。
     那份文件里 `### 2026-xx-xx` 开头的**日期章节是改动史,不必通读**,排查时按需 grep。

   **按需查(不必通读)**:`UE节点备忘录.md`(261KB 案例档案,拿"坑XX"编号去 grep 完整排查过程)、`UE蓝图状态.md` 的日期章节。

   不要假设"摘要里写了做过什么"就等于"文档是同步的"——历史上不是。
2. **每次 `compile_blueprint` 成功、确认一个改动生效之后,当场同步文档**,不是等任务/会话结束才补:
   - 改了变量/函数/EventGraph 结构 → 更新 `UE蓝图状态.md`
   - 踩到新坑(节点名、DSL 读写不对称、时序问题等) → 追加进 `UE节点备忘录.md`
   - 验证/验收了某个功能 → 更新 `UE测试用例.md`
   把这一步当成改动流程本身的一部分(改 → 编译 → 验证 → **写文档** → 下一步),不要放到"收尾才做"的位置——这正是 2026-08-29 那次多次误判、来回排查的根因(文档和真实蓝图状态脱节,靠聊天摘要在瞎猜现状),细节见 `UE节点备忘录.md` 坑62–64。
   **这一条现在有硬性执行兜底**:`.claude/settings.json` 配了 hooks——`compile_blueprint` 之后没同步 `UE蓝图状态.md`/`UE节点备忘录.md` 就想结束这一轮,Stop hook 会直接拦住(exit code 2)。看到拦截提示不要绕过去,先补文档。详见 `UE协作Harness规范.md` 1.5 节。
3. **新增/修改任何 UE 战斗规则(伤害、命中、反击、技能效果、状态…)之前**:先查 `UE规则对齐表.md` 对应行,确认这次改动会不会让某条规则和 web 版产生不一致。改完回来更新那一行。
   这份表存在的理由:此前没有任何地方写着"UE 版的目标规则集是什么",导致每次都靠临时判断——**AOE 会引发逐目标反击(web 完全不引)就是这么漏进去的**,没人决定过,是从 `TryAttack` 的实现细节里渗出来的。
4. **加新动画 / 换模型 / 排查动画朝向问题** → 走 `ue-add-animation/SKILL.md` 固化的流程,别凭记忆重新摸索一遍。
5. **大范围 Blueprint 图重构** → 走粘贴块生成器 **`ue/tools/paste_gen.py`**(pin 注册表 + 连线完整性校验,`python3 ue/tools/paste_gen.py --help`);**小范围改动**(接一根线、查/改单个节点)直接走 MCP。判断标准见 `UE协作Harness规范.md` 0.1/0.2 节。
   ⚠️ 规范里旧称的 `ue-blueprint-paste-gen` skill **在这个仓库里不存在**(`.claude/skills/` 没有这个目录),实际可用的就是上面那个脚本,2026-09-01 从遗留分支抢救进 main 并修好了 Windows 下的 cp1252 崩溃。

## 其它

游戏数值/平衡性调参见 `balance/SKILL.md`;自动试玩见 `autoplay-review/SKILL.md`;美术管线见 `UE美术管线.md`。项目背景、剧情、玩法设计等散落在根目录各 `.md` 文件,按文件名找,不在这里重复索引。
