# 纹兽战记 · 全项目工程与设计评估报告

> 评估日期:2026-08-31 · 评估范围:`js/`(web 运行时)、`balance/`(平衡台)、UE 蓝图移植(经 `UE蓝图状态.md` / `UE节点备忘录.md` 间接评估)、协作 harness 与仓库工程化
> 方法:通读全部 34 个 JS 文件(2213 行)+ `balance/` 7 个脚本(1444 行)+ 542KB harness 文档;实际执行 `node balance/difftest.js` 验证对拍现状;逐项核对文件行号,本报告中每条结论都可追溯到具体位置。
> 本文件是**评估结论与待办清单**,不是设计文档。执行某一条时请回到对应源文件确认现状(本报告本身也会过时)。

---

## 〇、一句话结论

**这个项目的设计密度和自我批判质量远高于它的工程结构能承载的水平。** 数值设计、恶意设计、深度量化这些"难的部分"做得相当扎实(有实验、有反例、有推翻自己的记录);但"同一套战斗规则被复制成四份、靠人力维持同步"这个结构性问题,正在把设计上的每一分收益都乘上一个衰减系数——它已经造成过真实的数值漂移(`rules.js:80` 有案底),现在还在继续制造新的分歧(AOE 一个机制就有四种行为)。

**如果这个项目只做一件事,应该是:把战斗规则收敛成一份实现。** 其余所有问题都是次要的。

---

## 一、值得肯定的部分(先说清楚哪些不要动)

这些是项目的真实资产,后续重构必须保留:

| 项 | 位置 | 为什么是资产 |
|---|---|---|
| **对拍机制的存在** | `balance/difftest.js` | 意识到"多份实现必然漂移"并主动建了检测,这个判断是对的。当前 2000 组样本 × 4 个公式 **✅ 全部一致**——它确实在工作。 |
| **设计决策带反例记录** | `config.js:14-21`、`sim.js:30-33` | "咽喉桥 1.0 太窄,漏斗化把位置选择消灭了而非创造"、"此前多数 A/B 结论差异 <10pp 都在噪声内"——**主动推翻自己的结论并留档**,这是大多数项目没有的。 |
| **数据/规则/表现分层的意图** | `rules.js:4-12` 的设计契约 | "无 DOM、无 await、随机性经 rng 注入、state 可深拷贝试走"——这是正确的架构目标,问题只在于执行成了"新增第三份"而不是"替换前两份"。 |
| **踩坑知识库** | `UE节点备忘录.md`(85 条) | 每条都有现象、根因、修法、可复用方法论。坑84(屏幕方向↔逻辑坐标轴换算)这类甚至是通用工程知识。 |
| **harness 硬性兜底** | `.claude/hooks/*.py` | 三个脚本都 fail-open、有 `stop_hook_active` 防循环、显式 `reconfigure(encoding="utf-8")` 修 Windows cp1252。质量高于一般的胶水脚本。 |
| **诚实的"未验证"标注** | 全部 UE 文档 | 严格区分"MCP 静态验证过连线"和"人工 Play 验收过",从不把前者说成后者。这个习惯极其可贵,不要弄丢。 |

---

## 二、P0 · 架构级问题(必须先解决,否则其它努力持续贬值)

### P0-1 · 同一套战斗规则存在**四份**独立实现

| 实现 | 位置 | 单位模型 | 服务对象 | 谁在用 |
|---|---|---|---|---|
| ① 游戏运行时 | `js/core/combat.js`(179 行) | `side:'player'\|'enemy'`,读全局 `units`/`TERRAIN`,async + DOM | 真实游玩 | `index.html` |
| ② 纯规则层 | `js/core/rules.js`(319 行) | `state.units`,无副作用 | 搜索/RL | **只有** `balance/arena.js`、`balance/debug_one.js` |
| ③ 平衡台 | `balance/sim.js`(624 行) | `u.player:bool`,模块级全局 | 所有平衡结论 | `sim.js` / `scan.js` / `agents.js` |
| ④ UE 蓝图 | `BP_GridManager` / `BP_Unit` | Blueprint 变量 | UE 版游戏 | UE 项目 |

**问题不是"有多份",而是这四份各自实现了不同的规则子集,并且没有任何机制能发现。**

#### (a) 权威性定义自相矛盾

- `rules.js:2` 自称 **"纯规则层(唯一真相源)"**
- `difftest.js:5` 写的是 **"js/core/combat.js (游戏,权威)"**

两份文件对"谁是真相源"的说法直接冲突。这不是文字问题——它决定了发现分歧时该改哪一边。**目前实际上是 combat.js 权威,rules.js 是个自称权威的旁支。**

#### (b) `rules.js` 根本没有被游戏加载

`index.html:101-133` 的脚本列表里**没有 `rules.js`**。这意味着:

- 那份被标注为"唯一真相源"、写了完整设计契约的 319 行代码,**对真实游戏零影响**;
- 它唯一的消费者是两个 Node 侧工具;
- 主平衡台 `sim.js` 也没用它(自带一份)。

**"抽象层"被加成了第三个副本,而不是替换掉前两个。** 这是本项目最贵的一次架构失误。

#### (c) 规则覆盖度实测对照(grep 计数,非推测)

| 机制 | `combat.js`(游戏) | `rules.js`(搜索/RL) | `sim.js`(平衡台) |
|---|:---:|:---:|:---:|
| ZOC / 借机攻击 | ✅ | **❌ 完全没有** | ✅ |
| `FLANK_NOCOUNTER`(夹击不可反击) | ✅ | **❌** | ✅ |
| `REST_HEAL` 脱战回复 | ✅ | **❌** | ✅ |
| 蓄力 `_charged` ×1.8 | ✅ | **❌** | ✅ |
| `gainShield`(技能加盾) | ✅ | **❌** | **❌** |
| `relicHitFix`(赌徒骰锁命中) | ✅ | **❌** | ✅ |

**后果**:任何基于 `rules.js` 的搜索 AI 或 RL 智能体,**玩的是一个和真实游戏不同的游戏**——它不知道脱离邻格会挨打(ZOC)、不知道夹击能封反击、不知道能撤下来回血。用它评估出的"技巧梯度""最优策略"全部失真。而 `AI路线_搜索与强化学习.md` 这条路线正是建立在 `rules.js` 之上的。

#### (d) AOE 一个机制,四种行为

| 实现 | 暴击 | 护盾吸收 | `dmgCap` | 异常状态 | 反击 |
|---|:---:|:---:|:---:|:---:|:---:|
| `combat.js:133-135` execAttack | ❌ 不掷暴击 | ✅ | ❌ 无视硬鳞 | ❌ | ❌ 不触发 |
| `sim.js:374-383` | ❌ | **❌ 直接 `d.hp-=b.d`** | ❌ | ❌ | ❌ |
| `rules.js:260-262` → `strike()` | **✅ 会暴击** | ✅ | **✅** | **✅** | ❌ |
| UE `PerformAoeSkillAttack` | — | — | — | — | **✅ 每个目标各反击一次** |

UE 版的 `PerformAoeSkillAttack` 对命中列表逐个调 `TryAttack`,而 `TryAttack` 在"防御方存活"分支末尾必定触发 `ResolveCounterAttack`(`UE蓝图状态.md:392`,**MISS 也会触发**)。所以:

> **UE 里放一个 AOE 打 3 个敌人,会吃到 3 次反击;web 里放同一个 AOE,一次反击都不吃。**

这不是实现细节差异,这是**玩法差异**——它会让 UE 版的 AOE 从"扫场神技"变成"自杀技",且没有任何人做过这个决定。

#### (e) 对拍的覆盖面远小于问题面

`difftest.js` 只比对 `baseDmg` / `hitRate` / `critRate` / `doubles` **四个纯函数**,且:

- **遗物/道具全部桩成中性**(`difftest.js:40-43`)——按构造,`relicHitFix` 这类分歧永远测不出来;
- **不覆盖** `strike`(伤害实际应用)、击退、AOE、反击、ZOC、状态结算、收服、经验、升级、进化;
- 用**字符串切片 + 花括号计数**从 `sim.js` 里抠函数体(`difftest.js:73-77`),用 `new Function` eval `combat.js`——重命名函数、或在字符串/正则里出现不配对花括号,就会静默失效或崩溃;
- **没有任何机制强制它被运行**(无 CI、无 pre-commit、无 npm script)。

**结论:对拍给了"三份实现一致"的安全感,但它实际保证的范围约占分歧面的 20%。**

---

### 建议方案(按投入从小到大,可分阶段)

**方案 A(最小可行,推荐先做)——不重构,先把对拍扩到真正的分歧面**

1. 把对拍从 4 个纯函数扩到**事件流级**:构造随机局面,三份实现各跑一次 `strike` / 完整 `applyAction`,比对产生的**事件序列**(伤害数、暴击、miss、状态、击退落点),而不只是比对中间数值。
2. 去掉"遗物桩成中性"这个前提,改成**注入同一组随机遗物**——这正是 `relicHitFix` 这类分歧藏身的地方。
3. 把 `pick()` 的字符串切片换成:给 `sim.js` 加一个 `module.exports`(Node 侧本来就是模块),直接 require。
4. 加 `package.json` + `npm test` 跑 difftest,并在 `CLAUDE.md` 里写死"改动任一份战斗实现后必须跑"。

> 验收标准:能主动复现出上表 (c)(d) 里的每一条分歧(先让它红,再决定每条怎么对齐)。

**方案 B(治本)——收敛成一份实现**

目标态:`rules.js` 成为**真正唯一**的规则层,`combat.js` 退化为"调用 rules + 播动画",`sim.js` 退化为"调用 rules + 统计"。

- `combat.js` 的每个 `async` 函数拆成两半:纯结算(交给 rules)+ 表现(动画/log/音效消费 rules 返回的事件流)。`rules.strike()` 已经返回 `ev[]` 事件流,这个接口设计是对的,直接用。
- `sim.js` 删掉自己的 `baseDmg`/`hitRate`/`critRate`/`strike`/`pushTargetS`/`losBlocked`,换成 `RULES.create(...)`。
- 先补齐 `rules.js` 缺的 6 个机制(见上表 c),补一个删一个副本,每步跑对拍。

> 成本估计:`combat.js` 179 行 + `sim.js` 624 行的改造,不是小工程,但每一步都可被对拍验证,可以增量做。
> 收益:此后新增任何机制(下一个 AOE、下一个遗物钩子)的成本从"改 3~4 处 + 祈祷"变成"改 1 处"。

**方案 C(UE 侧,单独说)**——见 P0-3。

---

### P0-2 · 战斗常量散落五处 + 静默 fallback 掩盖数据缺失

**现象一:同一组常量硬编码在 5 个地方**

```
js/core/state.js:7      const CRITX=3, DOUBLE_GAP=4, FOREST_AVO=15;   ← 游戏用的真值
balance/sim.js:27       const CRITX=3, DOUBLE_GAP=4, FOREST_AVO=15;   ← 平衡台自己抄一份
balance/_simfix.js:16   const CRITX=3, DOUBLE_GAP=4, FOREST_AVO=15;   ← 死文件里还有一份
balance/difftest.js:34  const CRITX=3, DOUBLE_GAP=4, FOREST_AVO=15    ← 对拍自己抄一份
balance/difftest.js:81  ... DOUBLE_GAP=4, FOREST_AVO=15, CRITX=3;     ← 同一文件里抄第二份
js/core/rules.js:23     D.CRITX!=null?D.CRITX:3                       ← 默认值又是一份
```

根因:这三个常量放在 `js/core/state.js`(**core 层**),而 `balance/loadData.js` 按设计只读 `js/data/*.js`(**data 层**),所以它取不到,所有下游只能硬编码。**这直接违反了 README:39-44 声明的"调数值只动 data/"分层契约。**

> **改 `CRITX` 会发生什么**:游戏立刻变化;平衡台、对拍、arena 全部继续用 3,且**不报错、不告警、对拍照样全绿**(difftest 只比 `critRate` 百分比,不比暴击后的伤害)。

**修法**(30 分钟,零风险):把 `CRITX` / `DOUBLE_GAP` / `FOREST_AVO` 三个常量从 `state.js` 挪到 `js/data/config.js`,加进 `loadData.js:9` 的 names 列表,删掉全部 5 处硬编码。

**现象二:`!=null ? : 默认值` 模式会静默吃掉数据缺失**

```js
rules.js:23   const _k = (D.DEF_K!=null ? D.DEF_K : 16)     // 真值是 9
sim.js:124    const _k = (G.DEF_K!=null ? G.DEF_K : 16)     // 真值是 9
rules.js:50   FLANK_M = (D.FLANK_MULT!=null ? D.FLANK_MULT : 1.15)   // 真值是 1.30
```

**这个坑已经真实发生过一次**,`rules.js:80` 留着案底:

> `// v0.71 修漂移:此处曾硬编码 1.15,漏跟 v0.58 的 FLANK_MULT=1.30 —— 由 difftest 对拍抓出`

fallback 默认值(16 / 1.15)全部是**过时的历史值**。一旦 `loadData` 少导出一个名字(比如新增常量忘了加进 `loadData.js:9` 那个 51 个名字的手写数组),三份实现会**静默地一起用错值**,而且用的是"曾经对过的旧值"——这是最难发现的一类错误。

**修法**:去掉所有 fallback,改成缺失即抛错(`if(D.DEF_K==null) throw new Error('DEF_K missing')`)。宁可炸也不要静默用旧值。

---

### P0-3 · UE 移植实际上是"重做一个规则更简单的游戏",但没有被当作这件事来管理

把 UE 侧接入的规则和 web 版逐项对照:

| 机制 | web | UE 切片 | 备注 |
|---|:---:|:---:|---|
| 防御折减公式 | ✅ `DEF_K/(DEF_K+Def)` | ✅ 同款 | `DEF_K=9` 两边一致 |
| 技能倍率 / 命中掷骰 | ✅ | ✅ | |
| 属性克制 | 18 属性完整克制表 | **4 属性三角,且当前关闭**(`bUseTypeChartInSlice=false`) | `UE蓝图状态.md:351` |
| 暴击 | ✅ ×3 | ❌ | |
| 二段攻击(速度差 ≥4) | ✅ | ❌ | |
| 护盾 / `dmgCap` / 硬鳞 | ✅ | ❌ | |
| 地形(森林掩体/高地+25%/岩浆/水) | ✅ | ❌ | |
| 视线遮挡 LOS | ✅ | ❌ | |
| 夹击 / ZOC | ✅ | ❌ | |
| 异常状态(灼烧/中毒/麻痹) | ✅ | ❌ | |
| 击退 / 落水处决 | ✅ | ❌ | |
| 反击 | ✅(有夹击豁免) | ✅(**无豁免,MISS 也反击**) | 行为不同 |
| AOE | ✅(不引反击) | ✅(**每目标引一次反击**) | 行为不同 |
| 遗物 | 30+ 种多类钩子 | 少量纯数值 | |

**这不是"移植进度 60%",这是两个不同的游戏。** 而项目文档里没有任何一处明确写着"UE 版的目标规则集是什么"——只有散落在各处的"切片阶段不做"。

**风险**:每加一个 UE 功能,都要临时判断"web 那边是怎么做的、这次要不要跟"。AOE 反击这条就是这么漏掉的——没人决定过它,它只是从 `TryAttack` 的实现细节里**渗出来**的。

**建议**:写一份 `UE规则对齐表.md`(可以直接用上面这张表),对每一行明确标注三选一:**① 已对齐 / ② 故意简化(写明理由)/ ③ 待办(排期)**。新增 UE 功能前先查这张表。这是所有 UE 相关工作里**性价比最高的一份文档**,成本约 1 小时。

---

## 三、P1 · Web 版具体缺陷(按严重度排序,均带文件行号)

### 1. 事件流程由 **UI 文案正则** 驱动 —— `js/core/run.js:97`

```js
ev.c.forEach(ch=>{ ... b.onclick=()=>{ ch.fn();
  if(document.getElementById('eventModal').style.display!=='none'
     && !/选择/.test(document.getElementById('evTitle').textContent))  // ← 这里
    finishNode(); };
```

判断"这个事件选项是否需要二级选人"的依据,是**读取 DOM 里的中文标题、正则匹配"选择"二字**。

- `pickMon(title, fn)` 的所有调用点都恰好传了含"选择"的标题(`'选择进食的精灵'` 等),流程才碰巧正确;
- 任何人改一次文案(比如"选择"→"挑选")、或做国际化、或新增一个含"选择"二字但**不是**选人的事件标题,流程立刻错乱——要么节点永不结束(卡死),要么 `finishNode()` 被调两次。

**修法**:`pickMon` 内部置一个显式标志位(如 `run._awaitingPick=true`),`finishNode` 读标志位,不读 DOM 文本。

### 2. 休整文案写死"30%",与实际回复量不符 —— `js/core/run.js:19`

```js
m.curHp = Math.min(m.maxhp, cur + Math.ceil(m.maxhp * ascRestHeal(run.ascSel||0)));
log('🏕 休整:全队回复 30% 生命。','#7fe0a0');   // ← 恒定写死 30%
```

`ascRestHeal(t) = max(0.15, 0.30 - t*0.02)`(`config.js:80`)。Ascension ≥1 时实际回复量已经不是 30%,但 UI **始终报 30%**。玩家在高难度下会持续收到错误信息,且这类"UI 撒谎"的 bug 极难被自己发现。

### 3. 两个"rest heal"语义完全不同却命名雷同

- `REST_HEAL = 0.12`(`config.js:56`)= **战斗中**脱离接战的喘息回血
- `ascRestHeal(t)`(`config.js:80`)= **地图上**休整节点的全队回血

两者无关,但名字几乎一样。已经在 `rules.js` 缺失 `REST_HEAL` 这件事上造成过混淆风险。建议改名为 `DISENGAGE_HEAL` / `CAMP_HEAL`。

### 4. `const DEF_K=09;` —— `js/data/config.js:53`

`09` 是**遗留八进制字面量写法**。当前靠非严格模式的 legacy fallback 才被当成十进制 9;一旦 `config.js` 被加上 `'use strict'`、被转成 ES module、或被任何打包器处理,这行会直接抛 **SyntaxError**,整个数据层加载失败。

**修法**:改成 `const DEF_K=9;`(一个字符,零风险)。

### 5. 同一概念两个键名:`uid` vs `id` —— `js/core/units.js:2` vs `:23,:24`

```js
poolEntry(p)        → { uid: uid++, ... }   // 池条目用 uid
mkEnemy(t,x,y,boss) → { id:  uid++, ... }   // 战斗单位用 id
mkBattleUnit(src)   → { id:  uid++, ... }   // 战斗单位用 id
```

同一个自增计数器,写进两个不同字段名。`agent_api.js` / `rules.js` 全部按 `id` 找单位,池条目按 `uid`。任何"从池条目直接查战斗单位"的代码都会踩空。目前靠"两者从不混用"侥幸成立。建议统一为 `id`。

### 6. `render()` 每次全量重建 88 个格子的 DOM —— `js/ui/board.js:2`

```js
function render(){ boardEl.innerHTML='';
  for(let y=0;y<ROWS;y++)for(let x=0;x<COLS;x++){ ... c.onclick=()=>onCell(x,y); ... } }
```

`render()` 在每次命中、移动、状态结算、击退后都会被调用(`combat.js` 里 10+ 处)。每次:销毁 88 个 DOM 节点 → 重建 88 个 → 重新绑定 88 个闭包 → 重新触发全部 `<img>` 加载。

后果不只是性能:**任何 CSS transition / animation 都会被打断重置**,这是"动画做不顺"的结构性原因。11×8 规模下还能忍,但这是后续做表现力时必然要还的债。

**修法**:格子 DOM 建一次,`render()` 只更新 `className` 和内部文本。

### 7. Agent API 靠 DOM 可见性轮询 + 静默超时 —— `js/ui/agent_api.js`

```js
function _scr(id){ return document.getElementById(id).style.display!=='none'; }
function _waitReady(ms){ ms=ms||9000; ... if(ready||Date.now()-t0>ms) res(window.snapshot()); ... }
```

- 判断"现在在哪个界面"靠读 `style.display`,判断"能不能操作"靠 150ms 轮询;
- **超时后不报错,直接返回一个可能不合法的 snapshot** —— 调用方无法区分"正常就绪"和"等了 9 秒放弃了";
- `_options()` 是**第四份**动作枚举实现(前三份:`rules.legalActions`、`sim._plans`、`ai.js`),且不含 heal / wait / ZOC 代价。

这套 API 是 `autoplay-review` 自动试玩的地基,它的不可靠会直接污染试玩结论。**至少要把超时改成 reject 或返回 `{timeout:true}`。**

### 8. 回合推进的异步模型只有一个 `busy` 守卫 —— `js/core/turn.js`

`nextActor()` 是 `async` 且内部有多个 `await delay(...)`,但 `advanceInit()` 调用它时**不 await、也不 catch**。`advanceInit` 被 `finishAct` / `execHeal` / `execSwap` / `execCharge` / `agent_api` 从同步上下文多处调用。整个并发正确性依赖单一全局 `busy` 布尔量,且 `busy` 的置位/复位散落在 `combat.js` 各处(`execAttack` 里有 3 处赋值)。

未观测到具体 bug,但这是"偶发重入 / 双重结算"类问题的温床,且极难复现调试。建议把回合推进改成一个显式的串行队列。

### 9. 存档无版本号 —— `js/core/save.js`

`serializeRun()` 写入 localStorage 的 JSON 没有 `version` 字段,`applyRunData()` 也不做任何校验。数据结构一旦变更(比如给池条目加必填字段),老存档会以"字段缺失 = undefined"的形式静默复活,产生难以归因的 NaN / 崩溃。**加一个 `v:1` 字段和一个版本检查,成本 10 行。**

### 10. 事件永久属性成长无上限 —— `js/core/run.js:23-84`

20 个事件里有 11 个是永久属性增益(`m.atk+=3`、`m.spd+=1`、`m.maxhp+=9`…),**没有任何上限**。`MAXLV` 限制了升级成长,但事件成长完全在体系外。理论上刷够事件节点可以无限堆属性。当前靠"地图深度 9 列、事件节点占比有限"隐式约束——这是巧合性平衡,不是设计约束。

### 11. 商店价格硬编码在 UI 层 —— `js/core/run.js:102-104`

`const price=50`(遗物)、`hp=30`(全队回满)、`ip=40`(道具)直接写在渲染函数里。这三个是**核心经济旋钮**,却不在 `config.js`。与金币产出 `run.gold+=12+chapter*3`(`sim.js:246`)也不在同一处。调经济需要在 UI 代码里翻。

---

## 四、P1 · UE 移植侧问题

> 以下结论来自 `UE蓝图状态.md` / `UE节点备忘录.md` 的自述记录,未直接连 UE 编辑器复核(MCP 当前连接失败)。

### 1. `BP_Unit.EventTick` 有 **5 份重复的同款逻辑**,且混着死代码

`UE蓝图状态.md:826` 记录:为了给瞄准态加门槛,需要**插入 10 个 `Branch(NOT Aiming)` 网关**——因为"移动"入口有 5 处、"射程/锁定高亮刷新"入口有 5 处,全是同一段逻辑的复制品(坑72 记为"只加不删"的历史产物)。同一段 Tick 里还确认存在:

- 一个 `Sequence` 节点自身 `execute` 输入没有任何连线 → 整段死代码;
- 一对被 Enhanced Input 事件独占供电、而 Enhanced Input 全局不触发 → 必然死代码的 `AddMovementInput`。

两段都被**刻意保留**("不在本次任务范围,风险未知")。

**这是 UE 侧最严重的结构问题**:任何 Tick 相关改动的成本已经 ×5,而且每次都要重新用 `get_node_infos` 回溯定位插入点(文档明说"花了大量回溯")。它会持续吃掉后续每一次 UE 工作的时间。

**建议**:把 EventTick 里这 5 份重复逻辑收敛成 1 个自定义函数 + 1 处调用,删除两段已确认的死代码。这属于 `CLAUDE.md` 定义的"大范围图重构",应走 `ue-blueprint-paste-gen` skill(带连线完整性校验),不要用增量 MCP 调用做。

### 2. Enhanced Input 全局不触发(坑41),整套输入建在 legacy 按键上 —— 且埋了一颗雷

现状:`IMC_TacticsControl` 里 `E` 键注册给了 `IA_EndTurn`(从未生效),而 legacy `E` 键事件绑的是**攻击**(`AttemptSkillAttack`)。

> `UE蓝图状态.md:174` 自己写明:**"如果以后修好了 Enhanced Input 触发问题,`E` 键会同时触发攻击和结束回合两件事"**。

这是一颗定时炸弹,且引信是"某天有人修好了另一个 bug"。**建议现在就把 `IMC_TacticsControl` 里失效的 `IA_Attack`/`IA_EndTurn` 映射删掉**(而不是留着),或者在 IMC 资产的注释里写死警告。成本几分钟,避免未来一次极难归因的排查。

### 3. 诊断 `PrintString` 留在正式版

- `OnAimPressed` 里 3 条(`UE蓝图状态.md:919` 明确写"暂时保留,等测试确认后再删")
- `AttemptSkillAttack` 里 5 条(`UE测试用例.md:109` 写"后续如果确认不再需要可以删掉")

两处都是"验收通过后再删",然后都没删。`bPrintToScreen=true` 的会直接糊在玩家屏幕上。**建议:建一个 `bDebugVerbose` 布尔开关统一门控,而不是靠记得删。**

### 4. 回归测试长期带伤运行

- **T7b / T7c 持续 FAIL**,被归类为"既有未解决的失败,历史遗留",推断由某次 TPS 迁移/AI 朝向改动引入,**未排期修复**;
- `StartTurn` 的 `Accessed None trying to read property Grid` 运行时警告被判定为"时序噪音,未继续深挖";
- T6a / T7b / T7c / T10f / T10h 存在**命中率随机导致的假阳性**,需要"重跑一次"——测试本身不确定;
- 测试地图里 4 个 `BP_Unit` **全部 `side=true`**(`UE蓝图状态.md:951`),场上根本没有敌方单位,任何涉及敌方的行为都无法被这套测试覆盖。

**一个长期有红灯的测试套件,等于没有测试套件**——因为没人再会因为红灯而停下。建议:①给 T7b/T7c 排期(要么修,要么明确标记 skip 并写明原因);②给随机性断言加固定种子或放宽到统计断言;③测试地图补敌方单位。

### 5. 蓝图里存在故意保留的孤立节点与重复函数

- `TryAttack` 里旧的 "ToFloat→DEF_K 公式→Round→Max" 整条节点链被**断开数据连接但保留在图里**(`UE蓝图状态.md:365`,"删除风险大于收益");
- `SkillIdToChineseB` / `SkillTokenToIdB` / `RelicCsvToIdsB` 三个函数留在蓝图里但主路径不再调用(`:440`);
- `ComputeSkillDamage` 的 `bUseSkill2` 参数**已是死参数**,实际读的是成员变量 `PendingSkillRowName`(`:140`),但签名还留着。

每一条单独看都合理(降低风险),累积起来就是"图里有一半东西不知道是活是死",后续每次排查都要重新判活(备忘录里专门有坑11"判活方法")。**建议在功能稳定期集中清理一次,而不是永久带着。**

### 6. AOE 技能列表在蓝图里**硬编码**,而不是数据驱动

`IsAoeSkill(RowName)` 是照抄 `IsPhysicalSkill` 的写法,**硬编码字符串比较** `"sweep"/"quake"/"rockslide"/"cleave"`。

根因在数据管线:`js/data/export_ue_csv.js:20` 导出的 CSV 表头

```
---,DisplayName,Mult,Hit,Crit,RangeBonus,TypeId,TypeName,InflictKind,InflictChance,bUseInflictInSlice,UePhase,bEnabledInSlice
```

**没有 `Kind` 列**(web 侧 `skills.js` 里每个技能都有 `kind:'atk'|'aoe'|'heal'`)。数据表里没有这个信息,UE 侧只能硬编码。

**后果**:加第 5 个 AOE 技能,需要改 Blueprint 函数(高摩擦、易出错),而不是加一行数据。

**修法**(明确、成本低):在 `export_ue_csv.js` 的 `skillHeader` 加 `Kind` 列 → 重新导出 → UE 侧 C++ `FSkillRow` 加对应字段 → `IsAoeSkill` 改成读表。**这一条建议优先做,它把一个持续性摩擦点变成一次性成本。**

### 7. 导出脚本硬编码绝对路径 —— `js/data/export_ue_csv.js:48`

```js
const ueImport = 'C:/Users/AI_Work/Documents/Unreal Projects/MyProject 5.8/Saved/Import';
```

换机器、换 UE 工程名、换盘符即失效,且失败模式是"静默写到一个新建的空目录"(`mkdirSync(recursive:true)` 会直接把路径建出来)。**改成环境变量 + 缺失时报错。**

### 8. CSV 与 DataTable 不自动同步

`UE蓝图状态.md:42` 记录:AOE 4 行是用 `DataTableTools.add_rows` **直接写进 DataTable 资产**的,CSV 只是"同步加了 4 行备档",并明确写着"**两者不会自动同步,以后再手改 CSV 记得也要手动同步一次 DataTable**"。

这是又一处"靠人记得"的同步点。**建议**:确立单向流 `combat_tables.js → CSV → DataTable(走 UE 导入)`,禁止直接写 DataTable;或者反过来,但必须选一个方向并写进 `CLAUDE.md`。

---

## 五、P1 · 协作流程与仓库工程化

### 1. 强制阅读的文档已达 542KB,逼近可用上限

| 文件 | 大小 |
|---|---|
| `UE节点备忘录.md` | 289 KB(85 条坑) |
| `UE蓝图状态.md` | 253 KB |
| `UE测试用例.md` | 30 KB |
| `CHANGELOG.md` | 96 KB |

`CLAUDE.md` 要求每次 UE 任务前必读前两份 = **542KB**。这已经超过舒适阅读量,而且还在**单调增长**(两份都是纯追加式)。

**后果**:实际行为会退化成"只读最近几节 + 靠 grep",而这恰恰是 harness 想防止的失败模式(靠不完整信息判断现状)。

**建议**:
- `UE节点备忘录.md` 拆成 **`UE硬规则.md`**(约 20 条已固化的"永远这样做/永远别这样做",必读,目标 <15KB)+ **`UE坑档案.md`**(85 条完整排查记录,按需 grep,不必读);
- `UE蓝图状态.md` 拆成 **`UE蓝图状态.md`**(当前快照:变量表/函数表/连线要点,必读)+ **`UE改动史.md`**(按日期的排查过程,归档);
- 硬规则和快照要**可覆写**(状态就该被覆写),历史档案才追加。

> 这条的收益是复利的:它同时降低每次会话的启动成本和误判概率。

### 2. hooks 只校验文件名,不校验内容

`on_docs_synced.py` 在任何 `Edit`/`Write` 命中 `UE蓝图状态.md`/`UE节点备忘录.md` 时清除哨兵——**加一个空行也算同步完成**。这个限制文档里已如实写明(`UE协作Harness规范.md` 1.5 节),不算隐瞒,但值得加固:

- 哨兵文件里记录**具体编译了哪个蓝图**(当前只记 `toolset=`),让提醒能说清"该写什么";
- 校验时要求文档新增行数 > N,或要求新增内容里出现该蓝图名。

### 3. 分支状态:所有工作堆在一个名字已经不匹配的分支上

```
cursor/fix-debug-loadout-empty-apply   ← HEAD,领先 main 9 个 commit,落后 0
main                                    ← 9 个 commit 之前的状态
```

这 9 个 commit 包含:TPS 直控迁移、锁定式索敌、AOE 范围技能、协作 harness、动画 SOP——**没有一个和分支名"fix-debug-loadout-empty-apply"有关**(那个 bug 在 `cdd1ac4` 就修完了)。

另有两个远端遗留分支:`cursor/setup-dev-environment-35ba`、`cursor/ue-step5-defk-formula-a3e8`。

**后果**:`main` 不是可信状态。任何人(包括未来的 AI 会话)从 main 出发都会拿到 9 个 commit 之前的世界。

**建议**:把当前分支合进 `main`(这是单人项目,不需要 PR 流程),之后**要么直接在 main 上工作,要么保证分支名与内容一致且及时合并**。

### 4. 无 `package.json` / 无 CI / 无 lint / 无自动化测试入口

- `balance/*.js` 全靠手敲 `node xxx.js` + 记环境变量(`PROFILE=human STARTLV=2 SEED=... node sim.js 500`);
- `difftest.js` 这个**唯一的正确性防线**,没有任何机制保证它被运行;
- `DEF_K=09` 这类问题,任何 linter 都会立刻报出来。

**建议**(约 1 小时):加一个 `package.json`,配 `npm test`(跑 difftest)、`npm run sim`、`npm run export-ue`;如果用 GitHub,加一个跑 `npm test` 的 workflow。

### 5. 死文件与重复文件

| 文件 | 问题 |
|---|---|
| `balance/_simfix.js`(161 行) | **零引用**(全仓库 grep 无结果),含第 3 份常量副本 |
| `wenshou-autoplay-review/SKILL.md` | 与 `autoplay-review/SKILL.md` **字节级完全相同**(diff 无输出) |
| `wenshou-autoplay-review.skill` | 疑似同一 skill 的第三种形态 |
| `review-report.html`(33KB) | 2026-06-20 的旧报告,已被后续工作覆盖 |
| `balance/scan_progress.txt` / `scan_single.txt` | 已在 `.gitignore` 里,但仍在工作区 |

**建议**:删掉前三项,`review-report.html` 归档或删除。

### 6. `README.md` 严重过时

`README.md:57-59`:

> **后续可加(尚未实现)**:遗物系统、Meta 跨局进度、更多怪物/进化、音效。

实际上 `js/core/relics.js`、`js/core/meta.js`、`js/ui/sfx.js`、`js/ui/music.js` **全都存在且在 `index.html` 里加载**。README 也完全没提 UE 移植(项目当前的主线工作)、`balance/` 平衡台、`rules.js` 规则层。

README 是新人(和新 AI 会话)的第一入口,当前它给出的世界观是**错的**。

### 7. 二进制素材直接入库

`.git` 已 46MB:`theme_inst.mp3`(6MB)、`theme.mp3`(6MB)、5 个 mp4(各 2~3MB)、`mewtwo-mixamo/` 下十几个 fbx/obj(各 1.5MB)、`eevee-hy3d.glb`(0.9MB)。

不算紧急(仓库还小),但这些文件**永远不会被 diff,却会被每次 clone 完整拉取**,且已经进历史、无法靠 `.gitignore` 补救。如果后续要加更多美术素材,建议现在就上 Git LFS 或把素材移出仓库。

---

## 六、设计与玩法层面的观察

### 1. 平衡结论的统计功效问题(项目已自知,但影响面可能被低估)

`sim.js:30-33` 自己记录:

> 同配置 N=120 重复跑,通关率在 **32.5%~48.3%** 间浮动(极差 15.8pp)⇒ 此前**多数 A/B 结论(差异 <10pp)都在噪声内**。定种子 + 加大 N 是唯一解。

这个自我修正非常好。但要注意它的**追溯影响**:`CHANGELOG.md` / `改动总结_v0.55-v0.71.md` / `验收报告_v0.67.md` 里,v0.62 之前的所有 A/B 结论都是在这个噪声水平下得出的。其中一些已经变成了现在的设计前提(比如"走位流遗物垫底 ⇒ 引入 ZOC + 夹击加强",`config.js:62-66`)。

**建议**:挑 2~3 条**当前仍在生效的核心设计决策**,用定种子 + 大 N 重跑验证一次。不用全部重验,但至少要知道地基是不是实的。

### 2. 深度与恶意设计这条线是项目的差异化优势

`研究综述_深度与趣味的衡量.md`、`loop工程_恶意与趣味指标.md`、`恶意设计方案.md` 这套"先定义可测量的趣味指标,再用模拟去验证机制是否产生了它"的方法论,水平明显高于一般独立游戏项目。`config.js` 里几乎每个旋钮都带着"为什么改、之前什么问题、实测数据"的注释,这是罕见的好习惯。

**唯一的风险是它和 UE 移植的脱节**:所有这些深度机制(位置经济学、ZOC、视线、地形)在 UE 版里**一条都没有**。如果 UE 版是最终形态,那么这条线的全部投入目前尚未转化。这需要一个明确的决定(见 P0-3)。

### 3. "UE 版做到什么程度"缺少一个明确的定义

从文档看,UE 侧的工作节奏是"用户提一个需求 → 做一个功能 → 验收 → 下一个"。这个模式让每一步都有反馈,是好的;但缺少一个**目标态定义**,导致:

- 无法判断"还差多少";
- 无法判断某个 web 机制该不该跟进(AOE 反击就是这么漏的);
- 容易在表现层(TPS 相机、瞄准态 UX)投入很多,而规则层始终停在切片状态。

**建议**:定义 UE 版的**垂直切片验收标准**——"一局完整战斗,包含 X/Y/Z 机制,能从头打到底",然后倒推还差什么。

---

## 七、行动清单(按建议顺序)

### 立刻做(每项 <1 小时,零或极低风险)

| # | 动作 | 位置 | 验收 |
|---|---|---|---|
| 1 | `DEF_K=09` → `DEF_K=9` | `js/data/config.js:53` | 加 `'use strict'` 也不报错 |
| 2 | 删死文件:`balance/_simfix.js`、`wenshou-autoplay-review/`、`wenshou-autoplay-review.skill` | 仓库根 | 全仓 grep 无引用 |
| 3 | 当前分支合进 `main`,清理两个远端遗留分支 | git | `main` 与 HEAD 一致 |
| 4 | 重写 `README.md`(补 UE 移植、balance 台、rules 层;删"尚未实现"的错误清单) | `README.md` | 新人读完能说清项目有几层 |
| 5 | 删掉 `IMC_TacticsControl` 里失效的 `IA_Attack`/`IA_EndTurn` 映射 | UE 资产 | 拆掉 E 键定时炸弹 |
| 6 | 休整文案改成按 `ascRestHeal` 实算的百分比 | `js/core/run.js:19` | Asc≥1 时文案与实际一致 |

### 本周做(半天到一天)

| # | 动作 | 收益 |
|---|---|---|
| 7 | **`CRITX`/`DOUBLE_GAP`/`FOREST_AVO` 迁到 `config.js`**,删 5 处硬编码,加进 `loadData.js` | 消除一整类静默漂移 |
| 8 | **去掉所有 `!=null?:` fallback 默认值**,改成缺失即抛错 | 消除"静默用旧值"这个最难查的错误类 |
| 9 | **写 `UE规则对齐表.md`**(直接用本报告第三节的表),每行标注 已对齐/故意简化/待办 | UE 侧每次决策不再靠临时判断 |
| 10 | **CSV 加 `Kind` 列**,`IsAoeSkill` 改成读表 | 加 AOE 技能从"改蓝图"降级为"加一行数据" |
| 11 | 加 `package.json` + `npm test`(跑 difftest)+ `npm run sim` / `export-ue` | 让唯一的正确性防线可被自动执行 |
| 12 | 事件流程改用显式标志位,不读 DOM 文案 | `js/core/run.js:97` |
| 13 | `_waitReady` 超时改为显式失败信号 | `js/ui/agent_api.js`,让自动试玩结论可信 |
| 14 | UE:诊断 `PrintString` 统一挂 `bDebugVerbose` 开关 | 不再靠记得删 |

### 本月做(有明确边界的中型工程)

| # | 动作 | 说明 |
|---|---|---|
| 15 | **扩展 difftest 到事件流级 + 注入真实遗物**(方案 A) | 目标:能主动复现本报告第二节 (c)(d) 的每一条分歧 |
| 16 | **拆分 harness 文档**(硬规则/坑档案、当前快照/改动史) | 把必读量从 542KB 压到 <50KB |
| 17 | **收敛 `BP_Unit.EventTick` 的 5 份重复逻辑**,删两段已确认死代码 | 走 `ue-blueprint-paste-gen` skill;此后 Tick 改动成本 ÷5 |
| 18 | 给 T7b/T7c 排期(修或明确 skip),测试地图补敌方单位,随机断言定种子 | 让回归测试重新变成"红灯必须停" |
| 19 | 用定种子 + 大 N 重验 2~3 条仍在生效的核心平衡结论 | 确认地基 |

### 更大的决定(需要先想清楚,再动手)

| # | 议题 | 关键问题 |
|---|---|---|
| 20 | **战斗规则收敛成一份**(方案 B) | 这是本报告的核心建议。建议在 #15 完成后再启动——先有能验证的对拍,重构才安全。 |
| 21 | **UE 版的目标规则集** | UE 是"web 的完整移植"还是"一个规则更简单的 3D 重制"?两条路都合理,但必须选一条并写下来。当前的模糊状态是 AOE 反击这类问题的温床。 |
| 22 | **深度机制线与 UE 线的关系** | ZOC/视线/地形/夹击这套"位置经济学"要不要进 UE?如果不进,`研究综述`/`恶意设计方案`这条线的定位需要重新明确。 |

---

## 附:本次评估的验证记录

为避免本报告出现"看起来像但没验证"的结论,以下是实际执行过的验证:

- ✅ `node balance/difftest.js 2000` → **全部一致**(所以"对拍在工作"是事实,"对拍覆盖不足"也是事实,两者不矛盾)
- ✅ `grep -c` 逐项统计六个机制在三份实现中的出现次数(第二节表 c 的数据来源)
- ✅ `diff autoplay-review/SKILL.md wenshou-autoplay-review/SKILL.md` → 无输出(字节级相同)
- ✅ `grep -rn "_simfix"` → 无结果(确认死文件)
- ✅ `grep -n "<script" index.html` → 确认 `rules.js` 不在加载列表
- ✅ `git rev-list --count main..HEAD` → 9;`HEAD..main` → 0
- ✅ `python3 --version` → 3.14.4(hooks 的 `python3` 调用在本机可用)
- ⚠️ **UE 蓝图部分未直接复核**:本次 `unreal-mcp` 服务器连接失败(ConnectionRefused),UE 侧全部结论来自 `UE蓝图状态.md` / `UE节点备忘录.md` 的自述记录。这些文档的可信度较高(有严格的验证/未验证区分习惯),但**第四节的每一条在动手前建议用 MCP 复核一次当前真实图结构**——这正是 harness 规范本身要求的做法。
