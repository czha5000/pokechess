# build 深度重构设计(StS 式:每战推进 + 深度缩放)

## 0. 为什么改(共识)
对比 StS,我们 build 浅在两点:① **频率**——大多数战斗只给经验(自动升级),不推进 build;② **深度**——遗物多是 +2 平摊、精灵 Lv8 封顶,没有"一条路越投越炸"的缩放轴。
**结论**:不压缩流派,反而要给每条路加"**每战推进 + 深度缩放**"。压缩是之前的错判,已纠正。

---

## 1. 核心立意:招式 = 我们的"卡组"
- **现状**:招式由 `LEARN[type][lv-1]` 按等级自动发,固定 → 没有"构筑"动作。
- **改为**:招式是**局内可 draft / 可升级**的"卡"。每只精灵有 N 个招式槽;胜利给"招式抉择"往槽里加/升。
- 遗物保留为**放大器**(StS 的 relic),招式才是**每战重塑的牌**。

---

## 2. 支柱一 · 每战三选一(频率)
**每场战斗胜利**(不再只精英/Boss)→ 弹「三选一」,来源加权:
- **学新招**:给某精灵加一个带流派标签的新招(进阶招池)
- **升级招**:已有招 +伤害 / +效果 / +射程(**可叠**,同招升 3 次=质变)
- **遗物**:原遗物池,协同加权(频率调低,保持稀缺)
- (保底)小强化 / 金币
→ 构筑从"偶尔"变"每战一步"。UI 复用现成的 relicModal 三选一。

## 3. 支柱二 · 深度缩放协同链(深度)
每条流派 = **4–6 件可在一局内逐步 collect 的拼图 + 一个随投入指数增强的缩放点**:
| 流派 | 缩放链(越投越炸) |
|---|---|
| 毒流 | 毒招叠层 → 遗物「每层毒额外+1伤」→ 招「引爆」(消耗毒层 ×N 伤) |
| 灼烧 | 多段点燃 → 「灼烧目标受击+50%」→ 连锁引燃 |
| 暴击 | 暴率叠加 → 「暴击触发追击一次」→ 暴击连锁 |
| 盾转攻 | 盾叠加 → 盾击按盾×2 → 滚雪球 |
| 走位 | 击退链 → 「撞击伤害随本回合击退数+」→ 控场连段 |
- **升级可叠**是深度的关键:把同一条路的件数 + 升级次数变成非线性强度。

---

## 4. 与现有系统对接(改动面)
- `SKILLS` 加字段:`tier`(基础/进阶)、`upLevel`(已升几级)、`scale`(缩放钩子)。
- 精灵加 `skillSlots`(如 4);draft 把招放进槽;升级改招的运行期数值。
- `combat.baseDmg/strike` 读招的升级值与缩放钩子(毒层、击退数等)。
- 遗物体系、协同加权抽取(刚做的)**保留**;流派标签 `arch` 复用。
- 平衡:用 v2 sim 盯 通关率/一招秒/成套率,新增「**build 深度分**」= 主流派件数 × 升级次数 × 缩放系数。

---

## 5. 分期(两根支柱都上)
- **P1 · 频率框架 + 可升级招**:每战三选一(学招/升招/遗物)跑通;招式可升级且**同招可叠**;先放 2–3 条缩放遗物验证"越投越深"。
- **P2 · 深度链补全**:各流派的缩放协同链(毒爆/灼烧连锁/暴击追击/盾雪球/走位连段)+ 进阶招池。
- **P3 · 收尾**:平衡调参 + 局内「当前主流派 / 深度」提示 UI + 文档。

> 风险:动核心循环,需小步快跑、每步过 sim。存档兼容:招式槽/升级状态要进 save。
