> ⚠️ **本文档的原始版本(下方"一、"起)写于实战之前。经过 v0.55–v0.69 共 15 个版本的实际使用,**
> **其中多条判据被证伪或需重新校准。请先读【第 0 章 · 实战修订】,它优先于下文的任何内容。**

---

# 第 0 章 · 实战修订(2026-08-02,经 15 个版本验证)

## 0.1 最重要的一条:先测尺子,再测游戏

**建指标体系的第 0 步,是测量指标自身的噪声。** 我做了 6 个版本的 A/B 才回头做这件事,导致那 6 个版本的多数结论作废。

实测:通关率在 N=120 时,同配置重复跑的极差达 **15.8pp**(48.3/40.0/32.5/37.5)。
⇒ 要可靠分辨 10pp 差异需 ~400 局,分辨 5pp 需 ~1500 局。

**规范**:
- 任何对比结论,**至少 3 个种子**,单次测量不作数
- 用 `SEED` 环境变量固定随机流(已实装)
- **报告效应量和极差**,不报单个数字

## 0.2 主指标必须是低方差连续量

| 指标 | 变异系数 | 相对标准误(N=120) |
|---|---|---|
| 通关率(二值) | 62-71% | 6.3% |
| **推进力(节点 0-27,连续)** | **18-25%** | **2.0%** |

⇒ **等效样本量需求降到约 1/9。** 二值指标(胜/负)在高方差游戏里是最差的选择。

## 0.3 测量必须在量程中段

天花板/地板会压缩效应,让真实差异测不出来:
- 默认难度下 smart 24/27、novice 21/27,两端都贴顶 ⇒ 梯度被压缩到 3.0
- **ASC=2** 下 18.5 vs 12.8 ⇒ 梯度 5.7,判别力恢复
- 同一个坑在遗物组合扫描时也踩过(单件已把胜率顶到 90%+,组合协同全为负)

**规范**:梯度类判据一律在 ASC=2 下测;默认难度只用于验收"通关率是否在带内"。

## 0.4 ★「AI 看不见新机制」—— 踩了三次,必须进流程

| 次数 | 机制 | 症状 | 真因 |
|---|---|---|---|
| 1 | ZOC 借机攻击 | 指标纹丝不动 | AI 评分函数无"脱离会挨打"项 |
| 2 | 近择率的位置维度 | 连续三版不动 | `_plans` 每个(招×目标)只留一个落点,**指标结构上看不见站位** |
| 3 | 中央高地 | 梯度不升 | 落点评分只认森林,**高地/岩浆完全不在其中** |

**强制流程**:任何涉及位置/地形/新资源/新动作的改动,**必须同步检查并更新 AI 的评分函数**,否则测量无效。
配套:为每个新机制写一个**能用性探针**(手搭场景,断言 AI 能选出该机制的正解)。这样才能区分"游戏浅"与"AI 不会用"。

## 0.5 调参前确认旋钮正交

`ENEMY_POWER` 曾同时缩放敌方**攻击和血量**:
- 想"降难度"就把它调低 ⇒ 敌人同时变脆 ⇒ 一招秒率不降反升
- 改完伤害公式后第一次调参,一招秒率只从 44.6% 动到 42.1%,**改动被自己抵消**
- 解耦(攻击归 ENEMY_POWER,血量归 ENEMY_HP_MUL)后,一举降到 17.0%

**耦合的旋钮会让改动互相抵消,且极难从结果反推。**

## 0.6 数值设计:减法项的代价曲线极陡

「狂乱契约」四次迭代,数据非常干净:

| 代价 | lift |
|---|---|
| 防御归零 | −12.6 |
| 防御减半 | −7.9 |
| 防御 −3 | −3.9 |
| **防御 −1** | **+2.04** |

因为伤害公式含减法项,防御每 −1 = 每次挨打 +1,**对每一次受击线性累加**。
⇒ **设计"代价"优先用乘法(受伤 +12%),慎用减法(防御 −N)。**

## 0.7 深度判据(四次实测归纳)

> **"增加深度" = 是否提供了多条有代价的路径。** 不是"机制更复杂",也不是"限制更多"。

| 设计 | 性质 | 梯度变化 |
|---|---|---|
| 熔岩裂谷 | 多路径 + 代价(耐烧的抄近路) | **+1.2** ✓ |
| 咽喉桥 v1 | **砍掉**路径(漏斗化) | 全场最浅 ✗ |
| 高地要塞 v1 | 单方面加成,玩家无选择 | **−2.3** ✗ |
| ZOC | 给移动加代价,但**没给新路径** | **±0** ✗ |

## 0.8 借来的目标带必须重新校准 —— 已有四个指标被证伪

| 指标 | 问题 | 处置 |
|---|---|---|
| 绝对胜率判超模 | 无对照(强制任意两件遗物都能到 89-97%) | 改测**相对偏离** lift/syn |
| 近择率 | ①"全调成等强"就能刷满分 ②**结构上看不见位置维度** | 降级为参考,不作门禁 |
| 死亡可避免率 | "零暴露格"在持久战里不现实 | 重定义为"**存在能活下来的落点**",31%→64% |
| 场均摆动 ≥1.5 | 恢复量(团队血 2.4%/次)比伤害量(10-15%/轮)小一个数量级,**短战斗里血线本就单调** | 目标带对本作不现实,应改测"最低血量水位分布" |

**教训:指标体系应该先用几个已知好/坏的版本做"标定实验"(sanity check)再定目标带,而不是直接从文献搬。**

## 0.9 一条改动一次验证

v0.61 三条改动一起上,导致归因困难,不得不临时加 `NOCHMAP`/`ONEMAP`/`NEWACT` 三个隔离旋钮才定位到真凶。
**排查旋钮要提前备好,改动要单条验证。**

---

# Loop Engineering:把「恶意」和「趣味」做成可优化的目标

> 承接《平衡测试方案》(A-D 组指标)与《恶意设计方案》。本文回答:难度已经进了"改→sim→批量确认"的循环,**恶意和趣味能不能也进循环?**
> 答案:能,但要先接受一条原则——**趣味不可直接测量,可测量的是趣味的前置条件**。指标是护栏不是靶心;指标负责否决,人负责拍板。

---

## 0. 三条纪律(防 Goodhart)

1. **全部用目标带,不用最大化**。任何"越高越好"的指标都会被循环钻空子(决策密度最大化=噪声;翻盘率最大化=橡皮筋)。
2. **指标只有否决权**:一个改动指标全绿≠好,人试玩点头才算过;但指标飘红=直接打回,不用试玩。
3. **每个指标必须写"作弊姿势"**(§6):知道它怎么被骗,才配进仪表盘。

---

## 1. E 组 · 恶意指标(测"敌人是否在出题")

关键洞察:**sim 的 `chooseAttack` 本来就给每个候选动作打了分**——决策空间的全部数据已经在内存里,只是没记账。以下指标几乎白拿。

| 指标 | 定义 | 目标带 | 实现点 |
|---|---|---|---|
| **被迫应变率** | 我方回合中,"本回合最优动作"因敌方上回合行为而改变的比例(对照:冻结敌方重算一次 best) | **25–45%** | chooseAttack 跑两次(真实/敌方冻结),比较 best 是否同一 (enemy,tile) |
| **集火压力** | 每个我方单位平均暴露在几个敌人的威胁区里 | **1.2–2.0** | `_threatAtS` 已有,按回合聚合 |
| **预告覆盖率** | 我方单位被打掉 ≥40% 血的打击中,"打击者上回合已在威胁显示范围内"的比例 | **≥90%** | 记录每次大额伤害时攻击者上回合位置 |
| **死亡可避免率** | 我方阵亡案例中,回溯其死前最后一个我方回合、枚举 top-3 替代动作,存在"能活下来"分支的比例 | **≥70%** | sim 快照该回合状态,替代动作各重放一次(sim 每局毫秒级,开销可忽略) |
| **单点失败集中度** | 已有(失败点分布),无单点 >12% | 保持 | 现成 |

- 被迫应变率 <25% = 敌人是背景板(现状);>45% = 玩家没有计划可言,只剩应激。
- **死亡可避免率是"恶意公平性"的唯一硬指标**:它直接回答《恶意设计方案》第 0 节第 2 问("回放能看出错在哪吗")。<70% 说明恶意在骰子里,不在设计里。

## 2. F 组 · 趣味指标(测"决策是否值得做")

| 指标 | 定义 | 目标带 | 实现点 |
|---|---|---|---|
| **近择率** | 我方回合中,存在 ≥2 个评分差 <15% 的候选动作的比例("真选择"=几个都想选) | **35–60%** | chooseAttack 记 best 与 second-best 分差 |
| **紧张度摆动** | 每场战斗队伍总血量%曲线的方向变化次数(单调下滑=0 摆动=碾或被碾) | 场均 **≥1.5** | 每回合记 teamHP%,数拐点 |
| **险胜率** | 队伍总血 ≤30% 时仍获胜的战斗占比 | **8–18%** | 战斗结束时记最低血量水位 |
| **翻盘率** | 曾跌破 35% 总血且最终通关的局占比 | **5–15%** | 局级聚合 |
| **构筑表达度** | 用 `RELICS=` 强制各流派各跑 100 局:各流派**动作分布**的差异度(用了哪些招/收服频率/站位倾向),而非仅通关率差 | 流派间动作分布明显可分 | env 旋钮现成,加动作计数 |
| **重玩差异度** | 相邻两局的(地图×目标×敌阵容)组合重复率 | **<30%** | 局级记录组合指纹 |

## 3. 技巧梯度(最重要的单一指标)

**趣味的底层是"决策有效"。决策是否有效,一测便知:让笨 AI 和聪明 AI 打同一套内容,通关率差距就是决策空间的深度。**

- 你们已经无意中做过一次:autoplay v1(贪心)78% vs v2(威胁加权)95%——17pp 的差距证明当时的决策是有效的。
- 正式化:sim 加 `POLICY=novice|smart` 旋钮(novice = 砍掉威胁规避/集火判断/残血撤退,只留贪心)。
- **目标带:smart − novice ≥ 25pp**。差距缩窄=新加的内容没有决策含量(比如纯数值 Ascension 就会缩窄它);差距拉大=恶意生效。
- **每次加"恶意"特性,这个数必须涨或持平**——它是恶意是否"逼出新决策"(第 0 节第 1 问)的自动化裁判。

## 4. 循环架构

```
设计改动(带假设:预期动哪个指标)
   │
   ▼
sim A/B:git 工作区跑 200 局 × {改动开/关} × {POLICY=smart/novice}
   │        (特性开关走 env,恶意特性做成 config.js 可开关旋钮)
   ▼
门禁规则(自动否决):
   ├─ 难度带:通关率 65-80、失败点无单点 >12%   ← A-D 组,现成
   ├─ 公平带:死亡可避免率 ≥70、预告覆盖 ≥90     ← E 组
   ├─ 趣味带:近择率 35-60、技巧梯度 ≥25pp        ← F 组 + §3
   └─ 任一飘红 → 打回重设计,不进入人工环节
   ▼
人工环节(指标测不到的:惊喜/挫败/节奏体感):
   ├─ 真人 3 局,每次死亡记一句"服气吗?为什么"
   └─ agent_api 让 LLM 实打 1 局,产出叙事复盘(它会抱怨,抱怨就是数据)
   ▼
通过 → CHANGELOG 记录:改动 + 指标前后对照 + 人工结论
```

## 5. 实现清单(按依赖序)

1. **sim.js 记账扩展**(半天):近择率/被迫应变率/集火压力/紧张度/险胜翻盘——全部挂在现有 `M.hits` 模式上,chooseAttack/battle 各加几行。
2. **POLICY=novice 旋钮**(2 小时):actPlayer 里跳过 `_threatAtS`/lethal 加分/残血撤退即可。
3. **死亡可避免率回放器**(1 天):battle 内每回合浅拷贝状态,阵亡时取上一快照枚举 top-3 重放。
4. **特性开关**(随恶意特性走):`config.js` 加 `FEATURES={focusFire:1,huntCarry:1,...}`,sim 用 env 覆写——A/B 的前提。
5. **仪表盘**:sim 输出加 E/F 两段,格式沿用现有报表。

## 6. 作弊姿势备忘(每个指标怎么被骗)

| 指标 | 循环会怎么钻空子 | 制衡 |
|---|---|---|
| 近择率 | 把所有选项调成一样强(全是 50 分)→ 噪声不是选择 | 同时盯技巧梯度:噪声会把梯度打平 |
| 翻盘/险胜率 | 敌人后期放水(橡皮筋)| 死亡可避免率不受益,且人工环节会骂 |
| 被迫应变率 | 敌人行为纯随机也能拉高 | 预告覆盖率会崩(随机=不可预判) |
| 技巧梯度 | 把 novice 调得更蠢 | novice 策略进 git 锁死,改它=改尺子,需评审 |
| 死亡可避免率 | 游戏整体变简单,没人死 | 通关率上限 80% 卡着 |

**结论:这六七个指标是互相制衡的一张网,单看任何一个都能被骗,一起看很难骗。这正是"趣味可工程化"的可行边界——网内自动化,网外交给人。**

## 7. 一句话版本

> 恶意 = 被迫应变率 × 公平性(可避免率+预告);趣味 = 近择率 × 技巧梯度 × 紧张度摆动。
> 全部设带、互相制衡、指标否决、真人拍板。sim 已有 90% 的地基,补的是记账,不是架构。
