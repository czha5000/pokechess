# 纹兽战记 → UE(2.5D)· 新手学习 + 移植路线图（教练模式）

模式：**学习优先**。我(教练)出骨架/讲原理/给分步任务与验收标准;**你在 UE 编辑器里动手**;卡住或报错贴给我,我帮你修、再带下一步。
原则:**蓝图优先**(熟了再按需 C++)、**先 2.5D 固定相机**、**范围切到最小**(先一场战斗,别想整局)。

---

## 我俩的协作回路(每个小任务都这样转)
1. 我给:本任务**目标 + 具体步骤 + 要学的 UE 概念 + 在哪查**。
2. 你做:在编辑器里实现(一次只啃一个概念)。
3. 你报:**截图 / 成功了 / 报错原文 / "卡在某步"**(截图直接贴,我能看)。
4. 我带:解释或修错 → 给下一个小任务。
> 关键:任务粒度要小。宁可十个 5 分钟的小步,不要一个 2 小时的大步。

---

## 阶段 0 · 装好 + 建立手感(约 1–2 周,业余)
**目标**:会用编辑器,理解蓝图(Blueprint)和 UMG 是什么——**先不碰我们的游戏**。
任务:
- 装 **Epic Games Launcher → 安装最新 UE5**。
- 新建工程:**Games → Blank → Blueprint(不要 C++)**,可含少量 Starter Content。
- 跟一个官方"入门第一小时 / Blueprint 新手"教程走一遍。
- 学:视口导航、放置 Actor、Blueprint 事件图(BeginPlay)、打印到屏幕、按键输入。
**验收**:做一个蓝图 Actor,运行时在屏幕打印一句话 / 按某键触发一个动作。

## 阶段 1 · 3D 网格 + 点击移动 + 斜俯视相机(约 2–4 周)
**目标**:搭出战棋的"地基"——这是 2.5D 最关键的一步。
任务:
- 用一个 Tile 蓝图,程序化铺出 N×M 网格(比如 11×8),记录每格的世界坐标(格子大小 = TileSize)。
- 放一个 Unit 蓝图(先用胶囊/方块占位)。
- **固定斜俯视相机**(一个 CameraActor 或简单 Pawn,正交或透视都行)。
- **点格子 → 单位移动到该格**(理解 网格坐标 ↔ 世界坐标 的换算)。
**验收**:点哪格,单位走到哪格;画面像一张战棋俯视盘。

## 阶段 2 · 移植"一场战斗"(约 1–2 月)
**目标**:把现有 `combat/turn` 逻辑搬成可玩的小遭遇战。
任务:
- **DataTable** 存怪物数值(从现有 `js/data/creatures.js` 转 CSV 导入)。
- 生成 2 我方 + 2 敌方;**按速度排行动序**(交错)。
- 选单位 → **高亮可移动范围(网格 BFS)** → 移动;选目标 → 结算伤害。
- 伤害/命中/暴击/克制/经验:做成**蓝图函数库**(或纯 C++ 函数),公式照搬现有 `combat.js`。
- 敌方基础 AI;**UMG** 血条 + 战斗预测面板雏形;胜负判定。
**验收**:一场 2v2 小战斗能完整打完。

## 阶段 3 · 单局结构(地图/部署/遗物/Meta/存档)
- 数据驱动 + 一个 RunManager;存档用 **USaveGame**。
- 章节地图、节点、事件/休整、遗物三选一、Meta 解锁——逐个搬。

## 阶段 4 · 美术与表现(2.5D 升级)
- 原创低面模型 / 简单动画 / 特效 / 音效(这是独立且最大的成本项,可外包/AI 辅助)。
- 相机可后续加"可旋转"。

## 全程并行
- **保留 Node 平衡台**:UE 里做个"把 DataTable 导出成 JSON 给 `balance/sim.js` 跑"的口子,平衡迭代不中断。
- 每完成一阶段,在 `CHANGELOG.md` 记一笔。

---

## 给新手的纪律(避免翻车)
- **别先写 C++**:全程蓝图,直到你明确需要性能/复用才引入 C++。
- **别做自由 3D 相机**:先固定斜俯视。
- **别一次导入一堆资源/系统**:一个概念一个任务。
- **别按 web 原型的速度预期**:第一个引擎 + 第一次 3D,慢是正常的。
- 我做不了的:替你操作编辑器、生成 3D 模型/蓝图二进制资源、做美术——这些你来(我给步骤/写纯逻辑代码/帮调试)。

---

## 现在的第一步
1. 装 Epic Games Launcher,安装最新 UE5。
2. 新建一个 **Blank · Blueprint** 工程。
3. 告诉我:**装好了、UE 版本号是多少、工程建好了没**。
然后我带你做阶段 0 的第一个小任务(屏幕打印 / 按键触发)。
