# UE 切片测试用例(验收清单)

> 每次改动后,把相关用例跑一遍,汇报 PASS/FAIL(一行就行,不用截图除非 FAIL)。全绿才能把对应任务标完成。
>
> **2026-08-15 起,这份清单只覆盖"需要人工点鼠标看视觉/交互表现"的用例。纯逻辑断言(比如某个 Function 在给定输入下算出的值对不对)优先写进 `BP_GridManager.RunRegressionTests`,跑法和当前覆盖的断言列表见 `UE蓝图状态.md` 末尾"自动回归测试"一节。** 两者互补:自动回归测试几秒内出结果、适合每次改动后马上跑;这份清单适合验证"看起来对不对""点起来顺不顺手"这类自动测试测不出来的东西。

## 移动
- [x] 点我方单位(不移动)→ 移动范围内格子高亮
- [x] 点高亮格子 → 单位移动过去,Z 轴不下沉,范围清除
- [ ] 点非高亮格子 → 不应该移动(待重新验证,状态机重构后再测;2026-08-15 已部分收窄——只有轮到的单位能点出高亮,见下方"回合"一节,但"移动/攻击模式互斥"仍未做完整状态机)
- [x] 移动目标格若有其他单位占用 → 不应该能选中(2026-08-15 已在 `ShowRange` 加 `IsTileOccupied` 占位检查,MCP 直接图编辑完成、编译通过,人工 Play 实测确认重叠格子不再高亮)
- [ ] **一回合只能移动一次**(2026-08-16 新增,用户 Play 实测反馈"现在可以无限移动,MoveRange 就没有意义了"):移动一次后再点自己单位,不应该再弹出黄/红高亮,点任何格子都不应该再次移动。已通过 `BP_Unit.bHasMoved` + `StartTurn` 重置 + `ActorOnClicked` 门槛 + `BP_Tile.ActorOnClicked` 置位四处协同实现,MCP 侧回归测试(11 条断言)重跑全 PASS,但这是纯交互行为,还没有人工 Play 验收过,见 `UE蓝图状态.md` 已知问题9。

## 回合(2026-08-15 大改:不再是"生成顺序 + N 键手动切",已重写为下面这套流程,见 `UE蓝图状态.md` `BP_TurnManager` 一节)
- [x] 行动顺序条 UI(`WBP_OrderBar`)在屏幕左上角正确显示,内容跟随 `RefreshOrder` 更新(2026-08-15 实测截图确认显示出占位数字;根因是 `AddToViewport` 必须放在 `BeginPlay` 而不是 `UserConstructionScript` 里才生效,踩坑细节见 `UE蓝图状态.md`/`UE节点备忘录.md`)
- [ ] `BuildTurnOrder` 真按 `Spd` 降序排序(不再是生成顺序),同速随机 tiebreak,每轮结束(`CurrentIndex` 超出数组长度)自动重排——待人工 Play 观察多轮顺序变化确认
- [ ] 只有 `TurnOrder[CurrentIndex]` 对应的我方单位能点出移动/攻击高亮,其余我方单位点击无效(逻辑已加在 `ActorOnClicked`,待人工 Play 确认体验符合预期)
- [ ] 攻击命中后自动 `EndTurn`;单纯移动(未命中攻击、或超出射程点了无效)不结束回合
- [ ] 敌方单位轮到时自动 `RunEnemyTurn` + `EndTurn`,连续多个敌方单位相邻时能一路自动跑完不卡住(2026-08-15 修复了 `StartTurn` 一段彻底断流的接线 bug,重跑全部回归断言确认无连带破坏,但这是"逻辑不断流"的自动化验证,"人工连续按流程走几轮观感顺不顺"仍待做)

## 攻击(Step5 ✅ 垂直切片已完成,伤害公式已换成真实版)
- [x] 点我方(不移动)→ 点相邻敌方 → 攻击链路执行(早期 Print 验证时数值 -30,是因为 HP 默认值当时未设,链路本身通;Class Defaults 补上后见下一条)
- [x] HP/Atk/Def/MaxHP 的 Class Defaults 数值已设置为合理测试值(2026-08-15 核实:HP=MaxHP=20,Atk=10,Def=5,Spd=6,MoveRange=5,AtkRange=2)
- [ ] 攻击范围红色高亮(`ShowAttackRange`)——点自己单位后屏幕上真的能看到一圈红格子(2026-08-15 自动回归测试 T8 只验证了 `AtkHighlighted` 布尔标记被正确置位,"视觉上真的渲染出红色"这一步仍需要人工在编辑器里 Play 验收——MCP 工具集目前没有 3D 视口鼠标点选能力,`SlateInspectorToolset.Click` 只能点 Slate UI 控件,不支持按世界坐标点选 actor)
- [ ] **红色高亮的语义已改两次**(2026-08-16):v3 把半径从 `AtkRange` 改成 `MoveRange+AtkRange`;用户接着反馈"移动范围和攻击范围重合时优先显示移动范围",v4 又加了下界 `>MoveRange`,让红色只覆盖"超出移动范围、在总范围内"的环形区域,不再盖住黄色。人工 Play 验收时确认:①移动范围(黄)完整可见,不被红色盖掉;②红色只出现在移动范围**外**、总范围(`MoveRange+AtkRange`)**内**的环形区域;③环外(超过 `MoveRange+AtkRange`)的格子既不黄也不红。见 `UE蓝图状态.md` `ShowAttackRange` v4 说明。
- [ ] 攻击后 HP 按新公式 `max(1, round(Atk×9/(9+Def)))` 正确变化,不再是占位版 `max(0,Atk-Def)`(2026-08-15 已切换实现,自动回归测试 T5 只确认"HP 确实下降"这一件事,没有断言具体数值;按 Atk=10/Def=5 手算应扣 6 血,这个数值本身还没有专门的自动断言或人工 Play 逐帧核对过,建议下次顺手验一下)
- [x] 超出 AtkRange 的敌方点击 → 无效果(根因是 SpawnUnit/BP_Tile 移动都没写逻辑坐标 Col/Row,修复后实测通过)
- [x] HP≤0 → 单位从场上移除(DestroyActor)——修复了 Target 未接 Defender、误伤 GridManager 自己的 bug 后确认正常
- [ ] 移动和攻击两个模式不会互相干扰(2026-08-15 部分收窄:加了"只有轮到的单位能点"门槛,减少了跨单位误触,但同一个我方单位自己回合内"点自己看范围"和"点敌方触发攻击"之间仍没有明确的状态机,完整的重新设计仍未做)

## 行动菜单(2026-08-16 新增,移动后弹"攻击/待命/撤销";2026-08-16 second round 修了一个真实 bug,见下)
- [x] **移动后弹出菜单**:点自己单位→移动到高亮格→高亮清除,屏幕上 `(500,400)` 位置附近出现三个按钮"攻击"/"待命"/"撤销"(用户实测确认弹出正常)
- [x] **菜单按钮真的能被点击**:至少"攻击"按钮确认能点到(否则不会有"点攻击再点敌人没反应"这个具体反馈)。`SetInputModeGameAndUI` 输入路由本身是通的。
- [ ] **点"攻击" → 菜单消失,接着点场上敌方单位应该正常触发 `TryAttack`**(2026-08-16 用户实测反馈"点攻击再点敌人没有反应"——已定位根因:`BP_Tile.ActorOnClicked` 移动收尾里有一条历史遗留的 `SetSelectedUnit(None)`,行动菜单上线后这个时机清空是错的,导致 `TryAttack` 读到 `SelectedUnit` 已经是 `None`、静默跳过整个函数体,`BP_Unit.ActorOnClicked` 敌方分支又误判成"已命中"直接 `EndTurn`。已删除这条清空、挪到 `StandbyAction` 里。**待人工 Play 重新验收**,见 `UE蓝图状态.md`"根因修复"一节和 `UE节点备忘录.md` 坑21)
- [ ] **移动后攻击范围红色高亮应该仍然可见**(2026-08-16 用户反馈"移动后仍要显示攻击范围"——已实现:新增 `BP_GridManager.ShowAttackRangeCurrent()`,移动收尾最后一步调用,以单位新位置为圆心、`AtkRange` 为半径重新画红圈,不再是"移动完两眼一抹黑"。**待人工 Play 验收**:移动落地后,菜单弹出的同时红圈是否正确围绕新位置显示。)
- [ ] 点"待命" → 菜单消失,直接结束回合(不用攻击),`CurrentIndex` 推进到下一个单位,行动顺序条同步刷新(2026-08-16 `SelectedUnit` 清空逻辑挪到了这里,待重新验收没有引入新问题)
- [ ] **点"撤销" → 菜单消失,单位视觉位置和逻辑 Col/Row 都回到本回合开始时的位置,并且可以重新点自己再移动一次**(2026-08-16 用户实测反馈"撤销后不能再行动,需要修复"——这次改动**没有直接针对性修复这条**,`UndoAction` 本身不清空 `SelectedUnit`,理论上不受本轮根因影响;如果人工重新 Play 验收后依旧复现,大概率是坑19记录的输入路由问题〔`SetInputModeGameAndUI`/`GameOnly` 切换未必对 3D 点击生效〕,需要另外排查,不要假设本轮改动已经顺带修好)
- [ ] 敌方回合(`RunEnemyTurn` 自动移动+攻击)不受行动菜单逻辑影响,不会意外弹出菜单或卡住
- [ ] **连续移动多个单位,点高亮格子应该始终一下就生效,不需要点两次**(2026-08-16 用户反馈"有时候移动点一下就可以有时候要双击"——根因是 `ShowActionMenu`/`HideActionMenu` 反复切换输入模式,已改成只切一次永久留在 `GameAndUI`,不再切回去,见 `UE节点备忘录.md` 坑25)
- [ ] **原地攻击**:点自己单位(不移动)→ 直接点相邻敌方单位 → 应该正常触发攻击(用的是"普通攻击"参数,因为没有经过菜单选择元素技能)(2026-08-16 用户反馈"不能原地攻击"——已修复,同上根因;附带修复了"原地攻击会用上一个单位残留的技能选择"这个次要 bug)

## 技能与遗物(2026-08-16 新增,最小闭环:固定2技能+命中率+四选一属性克制+2个纯数值遗物)
- [ ] **移动后菜单显示两个技能按钮**:"普通攻击"/"元素技能"(取代原来单个"攻击"按钮)
- [ ] 点"普通攻击" → 点敌人 → 伤害按 `mult=0.9·hit=95%·Normal属性(不参与克制)` 计算,`Output Log` 能看到 `HIT dmg=X`(命中)或 `MISS`(约5%概率)
- [ ] 点"元素技能" → 点敌人 → 预览数字和确认后 `HIT dmg=` 必须相同(同一套 C++ 公式)。当前 **属性克制关闭**,火打草不应再 ×2。Atk=12/Def=5 时 ember 约 12,不应秒杀 20 HP。克制表见 `属性克制配置.md`,打开 `bUseTypeChartInSlice` 后再测 2 倍/0.5 倍。
- [ ] 命中率会导致偶尔 MISS(不掉血,`Output Log` 打印 "MISS"),这是设计内行为,不是 bug,不要连续点很多次才发现"好像有时候没反应"就以为坏了
- [x] **遗物生效**:战斗一开始(`BeginPlay`)我方单位 `Atk`/`Def` 应该比 Class Default(10/5)高(切片默认 5 件:`power_band` Atk+2 等)。顶栏应显示遗物名,不再只能靠手算伤害间接确认(**2026-08-17 用户 Play 验收:开局有遗物**)
- [ ] 敌方 AI 攻击仍然只用普通攻击效果(没有元素技能/属性克制加成),这是已知简化不是遗漏

## 遗物顶栏 + Debug 控制台(2026-08-16)
- [x] Play 后顶栏应显示遗物（2026-08-17 用户验收:开局有 relic；APPLY 后不再被清空）
- [ ] Debug 面板技能/遗物应是下拉框（全中文名；诅咒带「（诅咒）」；遗物第一项「（空）」）
- [x] 点 DBG → 屏幕正中「选择配装」,能看到 **切片 / 重击流 / 元素流 / 应用自定义配装 / 关闭**（2026-08-17 用户验收通过）
- [x] 点 **重击流 / 元素流 / 应用自定义** 后遗物不再变 null,技能不再被空槽回退成开局 5 个（2026-08-17 用户验收通过）
- [ ] 移动我方单位,行动菜单五个技能按钮应显示 **重击 / 强袭 / 近身战 / 铁尾 / 咬碎击**,不是英文 id
- [ ] 重击流的强袭/近身战打满血敌人,预计伤害应明显高于切片的普通攻击(约 0.9 倍率 vs 1.7)
- [ ] 再点 **切片**,菜单应变回 **普通攻击/重击/火花/水枪/藤鞭**,顶栏回到切片 5 遗物
- [ ] 把技能 0 改成「近身战」,点 **应用自定义配装**,菜单第一个按钮应变成「近身战」(不是英文 closecombat,也不是空槽回退)
- [ ] 控制台打开时,点棋盘格子/单位仍应能点到(根 Canvas 是 `SelfHitTestInvisible`,只有面板自己吃点击)
- [ ] 连点两次同一预设,Atk 不应叠加(必须从 BaseAtk 重算)
- [x] 手填 CSV 只靠 Details 默认 Text:**已废弃**(GetText 经常空)。现改为 EventConstruct 运行时填下拉 + `SetText` 隐藏框。**APPLY 清空的验收根因不是 RelicCsvToIdsB prune**,而是 `GetComboOption` Return 没接 Exec + 空串覆盖隐藏框 + 无条件关 fallback(见备忘录坑35–37)。
- [x] 旧 RANDOM 抽切片 5 技能:**已换成 HEAVY 预设**(同一套 5 个技能再随机,肉眼也看不出差别)
- [ ] **伤害对齐**:选火花打满血敌人,预测「预计伤害」命中后应等于 `HIT dmg=` 和 `SETTLE dmg=`,约 12(切片遗物)或约 9(空遗物),敌人 20 HP 不应一招秒

## 反击 + 攻击预测(2026-08-16 新增,对齐火焰纹章反击 + 网页版伤害预测)
- [ ] **点敌方单位不再直接攻击**,而是弹出一个新的预测面板,显示"预计伤害: X (命中Y%)"和"预计反击: Z (命中W%)"(或"无反击")
- [ ] 预测面板上有"确认攻击"/"取消"两个按钮
- [ ] 点"确认攻击" → 面板消失,`Output Log` 能看到实际的 `HIT dmg=`/`MISS`,HP 按结果变化,回合正常结束
- [ ] 点"取消" → 面板消失,不发生任何攻击,可以重新点别的敌人或者别的操作
- [ ] **反击生效**:攻击命中或未命中后(只要防御方存活且在其自身 `AtkRange` 内),防御方应该自动反击一次(用普通攻击,不受防御方选没选元素技能影响),`Output Log` 能看到 `COUNTER dmg=`
- [ ] 反击导致攻击方 HP 下降,如果打到 0 攻击方应该死亡(和普通死亡逻辑一致,弹胜负判定)
- [ ] 距离超出防御方自己的 `AtkRange` 时(比如我方远程打近战、防御方够不着)不应该有反击,面板上应显示"无反击"
- [ ] **敌方 AI 攻击我方单位时,我方单位也应该自动反击**(全程不需要玩家操作,`RunEnemyTurn`→`TryAttack` 内部自动处理)
- [ ] (已知设计)预测面板上的数字是"假设命中"的固定值,不会因为随机数变化;真实结果只在点确认后才产生。**命中时**面板数字必须等于 `HIT dmg=`；MISS 时确认扣 0,面板仍显示假设命中值。
- [ ] (已知测试抖动,非 bug)自动回归测试 T5/T6a 偶尔因为 MISS 假性 FAIL,重跑一次即可,见 `UE节点备忘录.md`

## 待补(第6/7步)
- [x] 血条 UI 跟随 HP 变化(2026-08-15 完整实现并人工 Play 验收通过:`WBP_HealthBar` + `BP_Unit.HealthBarComponent`/`UpdateHealthBar`,`Setup` 时初始化满血、`TryAttack` 扣血后调用更新。过程中修了两个真实 bug——①Transform 类型 pin 不支持字符串默认值导致血条位置一直是 (0,0,0)、旋转也没面向固定相机,看起来是一条细线;②HP/MaxHP 整数相除截断成 0,导致随便掉一滴血就整条全红。两个都已修复,`RunRegressionTests` T5/T6a/T6b 三条断言覆盖。)
- [x] 一方全灭 → 胜负弹窗(2026-08-15 已实现并修过一个真实 bug、人工 Play 验收通过:`BP_GridManager.CheckVictoryCondition` 挂在 `TryAttack` 的死亡分支后面,全屏半透明遮罩 + 居中大字 "VICTORY"/"DEFEAT"(先用英文占位,没确认项目字体支不支持中文)。**中途踩过坑**:最初设计是一个共享 Widget 运行时 `SetText`,结果 `ConstructObjectfromClass` 造的实例控件树绑定没初始化,文字死活显示不出来(只看到灰色遮罩),报 `Accessed None`——已改成 `WBP_GameOver`(胜利)/`WBP_GameOverDefeat`(失败)两个独立 Widget,文字颜色全烤在设计时默认值里,不再运行时碰这个绑定,已用 MCP 直接读取 PIE 里的活对象确认文字("VICTORY")和颜色(绿色)都正确显示。详见 `UE节点备忘录.md`。人工 Play 已确认:连续攻击同一个敌方单位到死会正确弹出带文字的 "DEFEAT"/"VICTORY",且只弹一次不会重复弹。2026-08-15 又追加了 `bGameOver` 门槛,防止一方全灭后 `StartTurn` 继续推进导致 `Accessed None` 级联报错,见 `UE蓝图状态.md`。)
## TPS 直控迁移 · 阶段 A(2026-08-17,操控与相机地基;设计见 `C:\Users\AI_Work\.claude\plans\pokemon-tps-misty-walrus.md`)
- [x] **轮到己方单位时,按 WASD 能让该单位在棋盘上连续移动,身体朝向跟随移动方向转(2026-08-20 验收通过)**:Enhanced Input 全项目不触发的根因仍未定位(坑41,已搁置),`BP_Unit.EventTick` 里绕开它的 legacy 轮询方案转正为正式实现。经两轮修正——①移动方向参考系从 `GetActorForwardVector`/`GetActorRightVector` 改成 Controller `ControlRotation`(仅 Yaw),修好 A/D 打转和整体方向;②模型资产脸朝向是局部 +Y 不是 UE 约定的 +X,`UserConstructionScript` 里补 `StaticMesh.SetRelativeRotation(Yaw=-90)`,修好 W 走的时候身体侧对着移动方向的问题——详见 `UE节点备忘录.md` 坑42。用户人工 Play 验收通过。
- [x] 移动鼠标(或右摇杆)能转动第三人称相机(肩后跟随,约 300 距离/100 高度),角色身体不会跟着鼠标转(只有相机转,`bUseControllerRotationYaw` 等已关闭)——2026-08-20 验收通过,鼠标上下反向问题已修(`DeltaY` 插了 `×-1`,坑42)。
- [ ] 没轮到的己方单位、全部敌方单位应该**不可操控**(WASD/鼠标转视角对它们没反应),原有的鼠标点单位/点格子流程对这些单位应该还和之前一样
- [ ] 敌方 AI 回合(`RunEnemyTurn`)应该照常自动跑完,不需要也不应该触发 Possess
- [ ] 结束一次己方回合后(待命或攻击结束回合),原来那个单位应恢复不可操控;下一个轮到的己方单位才能用 WASD 移动
- [ ] (已知限制,阶段 A 暂不做)`IA_Attack`(左键)/`IA_EndTurn`(E)目前只在 Enhanced Input 里注册了映射,`BP_Unit` 还没接线,按了没反应,等阶段 C 才会生效

## TPS 直控迁移 · 阶段 B(2026-08-20,移动范围软边界 + Col/Row 回写;设计见同一份 plan 文档)
- [ ] **轮到己方单位时,离本回合开始点的移动距离(曼哈顿,不是直线)不能超过 `MoveRange×TileSize`(默认 5×100=500),且不能走出棋盘**:`BP_GridManager.ClampMovement()` 同时处理菱形移动范围边界(和 `ShowRange` 同款曼哈顿距离,不是欧氏圆形)+ 棋盘边缘硬边界(现算所有 Tile 的实际 min/max 坐标)。**2026-08-20 用户反馈第一版(欧氏圆形边界)"移动范围包含了攻击范围"且能斜着走出棋盘,已改成曼哈顿菱形边界并补上棋盘边缘限制**,详见 `UE蓝图状态.md`"TPS 阶段B"一节、`UE节点备忘录.md` 坑45。**MCP 已在 live PIE 里验证过**:斜向传送到移动范围外,拉回后精确落在曼哈顿距离=500 的位置(不再摸到攻击环);传送到棋盘外(不管多远),拉回后精确落在棋盘实际边缘(同一个数值,不随传送距离变化);`RunRegressionTests` 11 条断言(T1-T8)全 PASS。**待人工 Play 验收**:长按 WASD 斜向走到移动范围边缘,是否感觉是"菱形"边界(斜着走更早被挡住,不是走到黄色格子外面很远才被拉回);走到棋盘边缘时是否会被挡住出不去;范围内自由走动手感是否顺滑(不生硬卡顿/不抖动)。
- [ ] **移动后 `Col`/`Row` 正确同步、攻击范围/命中判定跟随实际站位**:`EventTick` 里新增 `GridManager.GetNearestTile()` 把连续移动后的世界坐标折算回最近的逻辑 Tile 并回写 `Col`/`Row`——这是阶段 A 完成时就存在的空白(一直没写回,只是当时没有测到)。**MCP 已验证** Col/Row 会跟随位置正确更新(界内/界外传送后分别读到了正确的最近 tile)。**待人工 Play 验收**:移动后再攻击,命中判定和伤害数字是否正常(和点击战棋模式一致);移动后攻击范围红圈(`ShowAttackRangeCurrent`)是否跟着新位置更新。

## ARPG 攻击操作(2026-08-21,数字键 1-5 选技能 + 鼠标左键瞄准攻击,取代原计划阶段C 的软锁定设计)
- [ ] **技能射程环随位置实时更新(默认技能)**:`BP_Unit.EventTick` 每 Tick 调 `Grid.ShowSkillRange(Grid.GetSkillEffectiveRange(self, SelectedSkillIndex))`,半径 = `Unit.AtkRange + 技能.RangeBonus`(数据早就在 `DT_Skills` 里,`aqua`/`vine`=1,其余=0,不用改表)。**MCP 已验证**:默认技能(index0=basic,RangeBonus=0)时,曼哈顿距离等于 `AtkRange` 的格子红色高亮、超出一格的不高亮,和预期精确吻合;`RunRegressionTests` 11 条断言全 PASS。**没有验证到的部分**(MCP 这轮没能让模拟按键接管 PIE 视口焦点):按 1-5 切换到 `aqua`/`vine`(4/5 号键,射程 = `AtkRange+1`)后,攻击范围环是否正确变大一圈。**待人工 Play 验收**:按 1/2/3(basic/heavy/ember,射程不变)和 4/5(aqua/vine,射程+1)时,红色攻击范围环大小是否正确切换;移动时范围环是否跟随角色实时更新,不残留错误位置的旧高亮。
- [ ] **鼠标左键沿准星瞄准攻击**:`BP_Unit.AttemptSkillAttack()` 从 `TPSCamera` 位置沿 `Controller` 的朝向做 3000 距离射线检测,命中的是敌方单位且在当前技能射程内就调用 `Grid.PerformSkillAttack()` 结算伤害并立刻结束回合(和原来点击弹预测面板的流程不同,这里没有确认步骤,命中就直接打)。**完全没有实测过**(射线追踪/瞄准精度是纯交互体验,MCP 没有办法模拟"鼠标准星对准 3D 场景里的某个单位"这个操作)。**待人工 Play 验收**:准星对准射程内的敌人,左键能否命中、正确扣血、正确触发反击(`TryAttack` 内建的反击逻辑应该照常生效)、正确结束回合;准星没对准任何单位或对准的敌人超出射程时,左键是否什么都不做(不报错、不空耗回合);准星对准我方单位时左键是否正确被拒绝(不能打友军)。
- [ ] (已知限制)`IA_Attack`/`IA_EndTurn` 这两个 Enhanced Input 映射从阶段A 起就没有真正生效(坑41)。2026-08-22 起 `E` 键已改用 legacy 按键事件接上了**攻击**(和鼠标左键并存,二选一都能触发 `AttemptSkillAttack`),不是原计划的"结束回合待命"——目前仍然没有"不攻击直接结束回合"的按键。

## 技能快捷栏 UI + E 键攻击(2026-08-22,用户反馈"没有UI显示攻击技能,屏幕下方应该有一排数字对应技能的 UI,类似 ARPG,然后按 E 攻击")
- [ ] **技能栏常驻显示**:屏幕底部居中应该始终能看到 5 个技能槽,依次显示"1 普通攻击/2 重击/3 火花/4 水枪/5 藤鞭"(或换装后对应的技能中文名),默认第 1 槽黄色高亮。**MCP 已验证**:PIE 截图确认底部正确渲染 5 槽 + 默认高亮(见 `UE蓝图状态.md` 对应小节截图描述),换装/预设按钮(`ApplyDebugLoadout`/`ApplyPresetSlice/Heavy/Elem`)理论上会通过 `RefreshSkillMenuLabels()` 联动刷新技能栏文字,但换装联动这部分**没有实测**,待人工 Play 验收。
- [ ] **按 1-5 切换高亮**:按数字键选中某个技能后,技能栏对应槽位应该变成黄色高亮,原来高亮的槽位应该变回常规深色。**2026-08-22 用户反馈"按1234没反应",排查发现 `UpdateSkillBarUI()` 第一版把 `IsValid` 写成了 `if` 条件位(错误语法),生成的是完全断连的死代码——已改用 `IsValid` 的 continuation 语法重写,`get_node_infos` 核对 exec 链路已完整连通(`Cast→IsValid(tm)→GetSkillBarWidget→IsValid(bar)→SetSelectedIndex`)**,详见 `UE蓝图状态.md`"当日修复"一节和 `UE节点备忘录.md` 坑51。**仍未实测**(MCP 没有可靠的键盘模拟能力),只保证了"exec 链现在是通的",实际按键后高亮是否真的移动待人工 Play 验收。
- [x] **按 E 攻击,鼠标完全不参与攻击/移动/菜单**(2026-08-22 用户 Play 验收通过):准星对准射程内敌人时按 `E` 键,应该命中扣血/触发反击/结束回合。**2026-08-22 用户反馈"不要鼠标点击攻击,完全改成按E攻击",反复三轮才堵完**,完整过程:
  - 第一轮删了 ARPG 阶段的 `LeftMouseButton→AttemptSkillAttack` 按键绑定,用户反馈"还是不行"。
  - 第二轮删了 `BP_Unit.ReceiveActorOnClicked`(点击 3D 网格)里"点敌方→弹攻击预测面板"的老分支,用户反馈"还是不行"。
  - 第三轮反向从 `TryAttack`/`ShowActionMenu` 这些终点函数搜索所有调用方,才挖出第三条最隐蔽的路径:`BP_TurnManager.StartTurn` 每次轮到我方单位会**自动**高亮移动/攻击范围格子,`BP_Tile.EventActorOnClicked` 一直监听"点一个高亮格子"→ 瞬移单位 + 弹出鼠标可点的行动菜单(`ShowActionMenu`)——这条链挂在"点地板格子(移动)"上,不挂在任何"攻击"相关事件上,前两轮排查都没往这个方向想。已用 `break_pins` 断开 `BP_Tile`/`BP_Unit` 两个 `EventActorOnClicked` 的入口线(不删节点,只断执行路径)。详见 `UE蓝图状态.md`"当日修复"第5条和 `UE节点备忘录.md` 坑52。
  
  **现状**:点击任何 3D 物体(自己的单位/敌方单位/地板格子)完全没有反应,`WBP_ActionMenu` 永远不会再弹出。`StartTurn` 自动高亮移动/攻击范围格子的视觉效果**还在**(纯装饰,点了没用,故意没动这个高风险的核心函数)。
  - **第四轮(真正的 bug)**:鼠标已经彻底没反应,用户反馈"仍然无法按E键攻击"。排查发现 `AttemptSkillAttack()` 的 `LineTraceByChannel` 终点算式里,"准星方向 × 距离"用的 `Math|Vector|vector*vector` 节点,第二个输入(距离)从这个函数第一次搭出来就**没有连线也没填值**,默认是零向量——导致射线终点等于起点,射线长度恒为 0,**从第一天起 `E` 键就不可能真正命中任何东西**,和这轮删鼠标的任何改动都无关,是历史遗留的真实 bug。已 `set_pin_value` 把这个输入设成 `(3000,3000,3000)`(沿准星方向延伸 3000 单位),`get_pin_value` 读回确认生效。详见 `UE蓝图状态.md`"当日修复"第6条和 `UE节点备忘录.md` 坑53。
  - **同时按用户要求加了 5 条 `PrintString` 调试日志**(屏幕+Output Log 都会显示,2秒):函数入口("E pressed, tracing...")、射线未命中任何东西、命中了但不是 `BP_Unit`(可能是地形/格子挡住)、命中了 `BP_Unit` 但 `ValidateSkillTarget` 判定无效(友军或超出射程)、`PerformSkillAttack` 真正触发。**Play 时注意看屏幕左上角或 Output Log 里这几条 `ATTACK:` 开头的文字**,能直接告诉你卡在哪一步,不需要再靠猜。

  **✅ 2026-08-22 用户 Play 验收通过**:左键点任何东西都没有反应,`E` 键能正常命中攻击。诊断用的 5 条 `PrintString` 先保留在 `AttemptSkillAttack()` 里(不影响正常游戏,只是屏幕上会多几行 2 秒的调试文字),后续如果确认不再需要可以删掉。

- [ ] 敌方 AI 自动移动+攻击(2026-08-15 自动化逻辑已完成:`FindNearestUnit0`/`MoveUnitTowardTarget`/`RunEnemyTurn` 三个函数本身由 `RunRegressionTests` 的 T7a/T7b/T7c 三条断言覆盖并全部 PASS。**过程中修了两个真实 bug**:①三个函数最初都用 `write_graph_dsl` 写的 `Utilities|IsValid` 判断,全部编译通过但静默断流/返回默认值,改成哨兵值/局部 bool 标记规避;②这三个函数以及 `RunRegressionTests` 反复用 `write_graph_dsl` 重写导致孤立节点堆积并被错误复用,改用 `remove_function_graph`+`add_function_graph` 清空重建解决,细节见 `UE节点备忘录.md`。**另外顺带发现并修复了一个更严重的问题**:`BP_TurnManager.StartTurn`(真正的游戏内入口,靠手工 `create_node`/`connect_pins` 拼图加的死亡跳过+AI分支逻辑)有一段接线是彻底断流的死路——不只是敌方 AI 调不到,连我方"点单位→`ShowRange`高亮"和`EndTurn`推进回合都会一起失效,只是没被 T7a/b/c 发现(那三条测试绕开 `StartTurn` 直接测 `RunEnemyTurn`)。已用 `get_node_infos` 核实并修好,重跑全部回归断言确认无连带破坏。**待人工 Play 验收**:按流程连续推进回合,确认①我方单位轮到时依旧能正常点鼠标选格子(验证上面这个断流 bug 真的修好了、没有回归,且新加的"只有轮到的单位能点"门槛没有误伤);②敌方单位轮到时无需任何操作,自动朝最近的我方单位移动、进入攻击范围就自动打一下,然后自动 `EndTurn` 轮到下一个;③连续多个敌方单位相邻时能不能一路自动跑完不卡住;④行动顺序条 UI 在敌方回合推进时是否也跟着正确刷新高亮。)
