# 纹兽战记 · 更强 AI 路线(搜索 → 强化学习)

> 目标:①现在——AI 够深,平衡数据才可信;②以后——有个正经的 RL 练手环境。
> 本文记录已建成的地基、**实测踩到的坑**,以及下一步该做什么。所有结论都有 arena 数据支撑。

---

## 一、已建成(可直接用)

| 文件 | 作用 |
|---|---|
| `js/core/rules.js` | **纯规则层**:无 DOM / 无 await / 可 `cloneState` 试走。含伤害·命中·暴击·移动 BFS·威胁度·**动作枚举 legalActions**·`applyAction(state,action,rng)`·`evalState`·`isTerminal`。浏览器挂 `window.RULES`,Node 走 `module.exports`,双端同源。 |
| `balance/agents.js` | **策略库**,统一接口 `act(R,state,unit)->action`:`novice`(尺子,锁死) / `greedy`(现游戏启发式) / `expectimax`(1层搜索,权重可 env 扫) / `exp_mc`(带概率采样)。**RL 模型将来实现同一个 `act()` 即可直接替换。** |
| `balance/arena.js` | **策略擂台**:同种子、同战场、同敌方 AI,只换我方策略。输出胜率/回合/存活/战后血/出手攻击%/伤害每动作/耗时。`CH=3 NODE_TYPE=boss node arena.js 60 greedy,expectimax` |
| `balance/debug_one.js` | 单局逐动作追踪;`DBGACT=1` 额外打印每个决策的**候选动作与评分分项**(delta/dealt/lost/kills/threat/stall/risk)。调 AI 必备。 |

`rng` 参数是设计关键:传函数=真随机(对局),传 `null`=期望值模式(确定性,给搜索)。

---

## 二、实测结果(第3章 elite,各 40 场同种子)

| 策略 | 胜率 | 均回合 | 存活 | 出手攻击% | 伤害/动作 | 耗时/场 |
|---|---|---|---|---|---|---|
| novice | 100% | 3.3 | 4.08 | 79 | 16.8 | 1.8ms |
| **greedy** | **100%** | **3.5** | **4.45** | 75 | 16.4 | 1.4ms |
| expectimax(1层) | 92.5% | 6.5 | 4.22 | 48 | 10.7 | 30ms |
| exp_mc(1层+5采样) | 95.0% | 5.4 | 4.28 | 51 | 11.0 | 129ms |

第3章 Boss:greedy 17.5% / expectimax 17.5%(调权重后),novice 10%。

**结论:1 层搜索没赢过调好的贪心。** 这不是实现 bug,原因见下。

---

## 三、踩到的四个坑(都有诊断记录,别重复踩)

### 坑1 · 归一化评估让"苟"成为最优解
`evalState` 用 `hp/maxhp` 求和:砍 Boss 20 血只动 0.09,自己挨 15 血动 0.375 → **防守权重是进攻的 4 倍**,AI 学会拖到 40 回合超时(实测均回合 36,存活 3 只但只赢 5%)。
**修**:改**增量**评估(待机≈0 分,不再积累存活价值)+ 加**绝对伤害进度项**。

### 坑2 · 1 层搜索看不见"回合上限=判负"
拖延零风险,所以永远最优。
**修**:显式**拖延税** `-(敌方剩余血量比例)×stallTax`,把时限编码进 1 层评估。

### 坑3 · `legalActions` 只给"最安全落点" → 永远后退
敌方 `mov` 被我们砍到 4(v0.52),追不上,双方拉锯到超时。
**修**:待机候选必须同时覆盖【最安全】与【最靠前】两端,评估里补**逼近奖励**。(已改进 rules.js)

### 坑4 · 期望值折叠 ≠ 击杀概率(**最贵的一个**)
追踪实录:鬼斯算出 `dealt=63, kills=1`(期望伤害恰等于耿鬼 63 血),于是冲到 (5,5) "稳拿人头";实际掷骰只打到剩 3 血,没杀掉,深入敌阵的鬼斯下回合被**集火秒了**。
期望伤害把 `90%×90%` 当成"必杀"。在高命中 + 永久死亡的游戏里,这种乐观会被反复惩罚。
**修**:概率节点采样(`samples>1`,对同一动作掷多次取均值)。实测 92.5%→95%、6.5→5.4 回合 —— **方向确认,但 1 层深度吃不满收益**。

### 附:一个容易误判的坑 · 边际威胁 vs 集中击杀
`enemyBestReply` 算敌方**总伤害**:我方单位踏进射程时总伤害几乎不变(敌人本来就要打别人),边际≈0 → 搜索看不见危险。变的是伤害**分布**——A1 集火协议会让全体扑向这个脆皮。
**修**:单列 `killRisk(unit)`:本回合未行动的敌人合力能否打死它。这是我们自己加的恶意在倒逼更聪明的解,符合预期。

---

## 四、为什么 1 层搜索赢不了贪心(根因)

1. **本作的战术深度在"回合内的多单位排序",不在"单个单位的选择"**。
   "A 先磨血 → B 补刀 → C 站位吃反击"这种配合,逐单位的 1 层搜索**结构上看不见**。greedy 因为总是打得最狠,反而无意中接近了正确解。
2. **当前平衡下,抢攻严格占优**。速度交错 + 减法伤害 + 一招秒率 39%(v0.53)⇒ 先手打死对面就没有反击。谨慎不被奖励,所以"更聪明"没有兑现空间。
   → 这本身是**平衡发现**,和 F 组「近择率仅 28%」互相印证:局面里真正的两难太少。
3. **擂台的目标函数偏向抢攻**:单场胜负下,牺牲单位是免费的;真实目标是**整局**(永久死亡有代价)。搜索的谨慎在 run 层可能更值 —— 但实测存活数 greedy 也更高(4.45 vs 4.28),所以暂时不能拿这个当借口。

---

## 五、下一步(按性价比排序)

### ▶ 第一优先:回合级(多单位)规划
不再"每个单位各自 1 层",改为**对整个我方回合做序列搜索**:
- 状态 = 本回合尚未行动的我方单位集合;逐个决策,后手能看到先手造成的结果(血量/站位)。
- 用**束搜索(beam search)**控爆炸:每步保留 top-K(K=8~16)个部分序列,按 `evalState + 伤害进度` 排序。
- 预期收益最大 —— 它正面解决"配合"这个结构性缺口,是 greedy 无论如何调不出来的东西。

### ▶ 第二:MCTS(在回合级规划之上)
- rollout 的 default policy 直接用 `greedy`(已经不弱),几百次模拟就能超过它;
- 天然处理概率节点(坑4),不用手工调"采样次数";
- 有了 `rules.js`,一次 `applyAction` 是微秒级,跑得动。

### ▶ 第三:接强化学习(学习目的)
地基已经全部就位,接 RL 只差环境封装:
- **接口**:`agents.js` 的 `act(R,state,unit)` 就是 policy 接口;RL 模型实现它即可无缝替换。
- **算法**:选 **PPO**(动作离散、可掩码);不选 DQN(组合动作空间不友好)。
- **状态编码**:11×8 多通道张量(我方/敌方/地形/威胁区/HP比例/本回合已行动),比手工特征泛化好。
- **动作掩码必须做**:直接用 `legalActions` 输出,否则 90% 探索浪费在非法动作上。
- **奖励**:`最终胜负 + 剩余血量比例 + 存活单位数`。只给胜负太稀疏;只给伤害会学出"送死换伤害"。
- **对照组现成**:novice / greedy / beam / MCTS 四条基线摆在 arena 里,RL 到底有没有学到东西,一跑便知。
- ⚠️ **RL 会专门学会利用 bug**(钓治疗者、卡地形)。这对"找漏洞"是优点,对"校准平衡"是污染 —— 别拿 RL 的畸形玩法当玩家真实水平去调数值。

### ▶ 配套:等价性验证(尚未做)
`rules.js` 与 `js/core/combat.js`、`balance/sim.js` 目前是**三份实现**。应写对拍脚本(同种子、同初始局面,比对伤害/命中/结算序列),确认三者一致后,**让 combat.js 与 sim.js 都改为调用 rules.js**,彻底消除漂移。这件事越早做越便宜。

---

## 六、给平衡工作的直接结论

- 现在**可以放心用 greedy 当基准玩家**——实测它在本作已接近该层级的最优,不是"太弱"。
- 真正让平衡数据失真的不是 AI 浅,而是**游戏本身缺少两难**(近择率 28%、抢攻占优)。做完《恶意设计方案》的地图/目标/诅咒遗物后再回来测,搜索型 AI 的优势才会显现。
- 换句话说:**AI 变强和游戏变深是同一件事的两面**。beam search 建议和恶意第二批并行推进,互相验证。
