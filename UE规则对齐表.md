# UE 规则对齐表

> **这份表回答一个问题:UE 版和 web 版的规则,每一条是"已对齐""故意简化"还是"漏了"。**
>
> 存在的理由:此前项目里没有任何地方写着"UE 版的目标规则集是什么",只有散落在各处的"切片阶段不做"。
> 结果是每加一个 UE 功能都要临时判断"web 那边怎么做的、这次要不要跟"——**AOE 反击那条就是这么漏的**
> (没人决定过,它只是从 `TryAttack` 的实现细节里渗出来的)。
>
> **新增/修改任何 UE 战斗功能之前,先查这张表。**做完之后回来更新对应行。
>
> 配套:`UE蓝图状态.md`(蓝图现状)、`UE节点备忘录.md`(踩过的坑)、`UE测试用例.md`(验收)、`工程评估报告.md`(为什么需要这份表)

---

## 状态标记含义

| 标记 | 含义 | 要不要做 |
|---|---|---|
| ✅ **已对齐** | UE 行为与 web 一致(允许数值精度差异) | 不用动 |
| 🔸 **故意简化** | UE 明确不做,**且写明了理由** | 不用动,但理由要能站住 |
| ⏳ **待办** | 应该做,只是还没排到 | 排期 |
| ⚠️ **不一致** | 两边都实现了,但**行为不同**,且不是有意为之 | **优先处理**——这类最危险,因为看起来"做了" |
| 🐞 **UE 内部矛盾** | UE 自己前后不一致(比如 UI 承诺了结算不兑现) | **优先处理** |

---

## 一、⚠️🐞 需要优先处理的(行为不一致 / 自相矛盾)

### 1. ✅ 已止血(2026-09-01)· 暴击:预测面板曾显示暴击率,而结算永远不暴击

| | |
|---|---|
| **web** | `crit` 掷骰,命中则伤害 ×`CRITX`(=3),受 `dmgCap` 限制 |
| **UE 结算** | `ComputeSkillDamage` 的公式**明确不含暴击**(`UE蓝图状态.md:135`:"不含技能倍率/属性克制/命中率/**暴击**/掩体/地形/遗物等乘算项") |
| **UE 显示** | `GetSkillCritChance` 读 `DT_Skills.Crit` 列 → `RefreshAttackForecast` 拼进"我方: 伤害X 命中Y% **暴击Z%**"显示给玩家(`UE蓝图状态.md:967,969`) |

**数据表里有 16 个技能带非零暴击率**,不是全零:

```
夜阴 15% · 飞叶刃 12% · 铁尾 10% · 咬碎击 10% · 气旋斩 10% · 近身战 10%
强袭 8% · 重击/暗影球/空手劈/金属爪/咬碎/极冰/龙波/魔法闪光/虫鸣 各 5%
```

**后果**:玩家装了夜阴,面板告诉他"暴击 15%",打一百次一次也不会暴击。**这是游戏对玩家撒谎**,比"没做暴击"严重得多。

**已做:(b) 摘掉显示(2026-09-01)**。`RefreshAttackForecast` 的两条 Append 链已改成只拼 `"…伤害X 命中Y%"`,暴击段移除,面板不再承诺兑现不了的东西。改法和保留项见 `UE蓝图状态.md` 2026-09-01 那节第 1 条。

**仍待做:(a) 补齐结算** —— `ComputeSkillDamage` 加暴击掷骰(`RandomIntegerInRange(0,99) < Crit` → 伤害 ×3)。工作量小(两个 `GetSkillCritChance` 调用**刻意保留在 exec 链上**了,直接把返回值接回去即可),但会显著提高伤害方差,20 HP 的单位更容易被秒,**需要重新验平衡**,所以单独排期。做完之后回来把显示加回去,两边要同时改。

---

### 2. ✅ 已修(2026-09-01)· AOE 曾会引发逐目标反击,web 完全不引反击

| | |
|---|---|
| **web** | `execAttack` 的 `aoe` 分支独立结算每个目标,**不触发任何反击** |
| **UE** | `PerformAoeSkillAttack` 对命中列表逐个调 `TryAttack`,而 `TryAttack` 在"防御方存活"分支末尾**必定**触发 `ResolveCounterAttack`(`UE蓝图状态.md:392`,**MISS 也触发**) |

**后果**:UE 里一个 AOE 打 3 个敌人 → 吃 3 次反击;web 里同一个 AOE → 一次不吃。AOE 从"扫场神技"变成"自杀技"。

**这条从来没有人决定过**,是实现细节渗出来的副作用。

**已按低风险方案实现(2026-09-01)**,没有改任何函数签名(规避坑66):
- `BP_GridManager` 新增 `bSuppressCounter`(Bool,CDO 默认 false)。
- `ResolveCounterAttack` 入口加一道 `Branch(NOT bSuppressCounter)` 闸门,原有的 `(and HP>0 距离<=AtkRange)` 判定完全没动,只是被包在外层。
- `PerformAoeSkillAttack` 入口 `Set true`,`ForEachLoop` 的 `Completed` 出口(原本空着)`Set false`。

**验证**:`RunRegressionTests` 跑过,T10a–i / T11a / T11b 全套 AOE 断言依旧全绿(反击伤害打在攻击者身上,不影响这些断言对目标掉血的检查);无新增失败。**仍待人工 Play 验收**:放一个 AOE 打多个敌人,确认攻击者不再连吃多次反击;以及单体攻击的反击**没有**被误伤(仍应正常触发)。

---

### 3. ⚠️ 反击没有"被夹击则无法反击"的豁免

| | |
|---|---|
| **web** | `FLANK_NOCOUNTER=1`:被夹击的目标**无法反击**(`combat.js:140-142`),这是"背击"的设计意图 |
| **UE** | 无夹击概念,反击条件只有"存活 + 距离 ≤ 反击者 AtkRange" |

依赖于夹击(见第三节),夹击没做 → 这条自然也没有。**归类为"待办",不算 bug**,但要记着:做夹击的时候必须连这条一起做,否则夹击只有加伤没有战术意义。

---

### 4. 🐞 UE 内部有两套伤害预览公式

| 函数 | 公式 | 用在哪 |
|---|---|---|
| `PredictDamage` | `ATK/(DEF+9)` —— **不含技能倍率/克制/暴击** | 旧的 `WBP_DamageForecast`(`ShowDamageForecast`) |
| `PreviewSkillDamage` / `PreviewAttackForecast` | 与 `ComputeSkillDamage` 同款完整公式 | 新的 `WBP_AttackForecast`(`RefreshAttackForecast`) |

`UE蓝图状态.md:962` 自己标注为"坑76 附近的历史遗留"。两套面板的数字会对不上。

**修法**:确认 `WBP_DamageForecast` 那一套是否还在用;不用了就连同 `PredictDamage`/`ShowDamageForecast`/`ConfirmPendingAttack`/`CancelPendingAttack` 一起清理(文档记载 `Btn_Confirm`/`Btn_Cancel` 已被强制 `Collapsed`,说明已经废弃但没删)。

---

## 二、✅ 已对齐

| 机制 | 说明 |
|---|---|
| **防御折减公式** | `max(1, round(Atk × SkillMult × TypeMult × DEF_K/(DEF_K+Def)))`,`DEF_K=9` 两边一致 |
| **技能倍率** | UE 读 `DT_Skills.Mult`,与 `js/data/skills.js` 同源(经 `combat_tables.js` → CSV 导出) |
| **命中掷骰** | `RandomIntegerInRange(0,99) < HitChance` 才命中;命中率读 `DT_Skills.Hit` |
| **技能射程加成** | `GetSkillEffectiveRange = Unit.AtkRange + DT_Skills.RangeBonus`,与 web 的 `u.rng + sk.rb` 一致 |
| **反击基本规则** | 目标存活 + 距离 ≤ 反击者自身 `AtkRange` → 用**普通攻击**反击一次(不是被选中的技能),与 `combat.js` 一致 |
| **反击也发生在敌方回合** | 敌方 AI 攻击我方时我方自动反击,与 web 一致 |
| **MISS 也会被反击** | web 的"你打空了对方还是能反击"设计意图,UE 行为一致 |
| **AOE 命中范围** | 主目标 + 正交相邻(曼哈顿距离 = 1)的敌方单位,与 `combat.js`/`rules.js` 的取列表逻辑一致 |
| **最低伤害兜底** | 两边都是 `max(..., 1)` |

---

## 三、🔸 故意简化 / ⏳ 待办

> 判断依据:**这条机制不在,游戏还成不成立?**成立 → 故意简化;不成立(比如设计文档专门为它写过论证)→ 待办。

| 机制 | web 有什么 | UE 状态 | 判定 | 理由 / 排期依据 |
|---|---|---|---|---|
| **属性克制** | 18 属性完整克制表(×2 / ×0.5 / ×0 免疫) | 只有 4 属性三角(火→草→水→火),**且 `bUseTypeChartInSlice=false` 当前关闭** | ⏳ **待办** | 关闭的理由是"打开后 20 血可能被秒"(`UE蓝图状态.md:581`)——这是**数值问题不是机制问题**,靠调 HP/倍率能解决。属性克制是这个游戏的核心识别度,长期不能没有。**建议:先把单位 HP 提上去,再打开开关,而不是一直关着。** |
| **暴击** | ×3 倍,受 `dmgCap` 限制 | 结算没有(但 UI 已显示,见第一节 #1) | ⏳ **待办** | 见第一节 #1,建议先止血再补齐 |
| **二段攻击** | 速度差 ≥ `DOUBLE_GAP`(4)时攻击两次 | 无 | ⏳ **待办** | 这是速度属性**唯一**的战斗意义。不做的话 `Spd` 在 UE 里只影响行动顺序,单位数值设计里的速度维度基本失效 |
| **异常状态** | 灼烧(固定伤×回合)/ 中毒(层数)/ 麻痹(概率跳过) | 无(`DT_Skills` 已有 `InflictKind`/`InflictChance` 两列但未接线) | ⏳ **待办** | **数据已经在表里了**,接线成本低于从零设计。而且 `bUseInflictInSlice` 这个开关列已经预留好了 |
| **护盾 / dmgCap(硬鳞)** | 护盾有上限、盾击类技能出手即清空;Boss 有伤害上限 | 无 | 🔸 **故意简化** | 护盾是"资源管理"层的机制,UE 当前连遗物系统都只有纯数值版,没有承载它的上下文。等遗物系统做深了再说 |
| **地形**(森林掩体/高地+25%/岩浆/水) | 4 种地形各有效果 | 无(棋盘是纯平地) | ⏳ **待办** | ⚠️ **这条最值得重新考虑**:`config.js:62-66` 记录了整套"位置经济学"的设计论证——引入 ZOC/夹击/视线正是为了让"站哪儿"有意义。UE 版做成了 TPS 直控(玩家亲自走位),**如果地形没有效果,直控带来的走位自由就是没有回报的**。见下方"战略建议" |
| **视线遮挡 LOS** | 远程穿过森林/单位:命中 −20、伤害 ×0.8;站高地无视 | 无 | ⏳ **待办** | 同上,依赖地形 |
| **夹击** | `FLANK_MULT=1.30` 加伤 + 被夹击无法反击 | 无 | ⏳ **待办** | 同上。TPS 直控下"绕到侧面"是玩家会自然尝试的操作,没有回报会显得很奇怪 |
| **ZOC 借机攻击** | 脱离敌方邻格会挨一记免费普攻(双向) | 无 | 🔸 **故意简化(暂)** | ZOC 的设计前提是"回合制下移动是一次性承诺"。UE 改成了实时直控移动,**ZOC 在直控下的语义需要重新设计**(什么时候算"脱离"?),不是简单移植。**建议:直到 UE 的移动模型定型前不做。** |
| **击退 / 落水处决** | 击退 N 格,撞墙/撞人/落水各有结算 | 无 | 🔸 **故意简化** | 依赖地形(水)+ 物理位移,成本高、收益局限于少数技能 |
| **脱战回复** `REST_HEAL` | 本回合未攻击且邻格无敌人 → 回 12% 最大生命 | 无 | ⏳ **待办** | 成本很低(一个 Branch + 一次加血),而 web 侧引入它是为了治"血线单调下滑"(`config.js:54-55`),UE 版同样会有这个问题 |
| **蓄力 / 换位** | `CHARGE_MULT=1.8`;交换两单位位置 | 无 | 🔸 **故意简化** | 属于"行动菜单时代"的动作,UE 已改成 ARPG 直控 + 技能栏,这两个动作没有对应的输入位置 |
| **遗物** | 30+ 种,多类钩子(击杀/命中/回合开始/条件触发) | 少量纯数值(Atk/Def/HP 加成) | 🔸 **故意简化** | 明确的阶段性决定,`combat_tables.js` 里 `uePhase` 列已经给每个遗物标了实现阶段 |
| **收服** | 贴脸收服,概率随目标残血上升 | 无 | ⏳ **待办** | 这是"怪物收集"这个品类标签的核心动词,长期不能没有 |
| **经验 / 升级 / 进化** | 完整成长曲线 + 两段进化 | 无 | ⏳ **待办** | 同上,Roguelike 单局成长的主循环 |

---

## 四、数据管线现状(补充,不是规则但影响改动成本)

```
js/data/skills.js  ──┐
js/data/relics.js  ──┤ 手工同步(⚠️ 无自动校验)
js/data/config.js  ──┘
        ↓
js/data/combat_tables.js   「纯数值」展平表
        ↓  node js/data/export_ue_csv.js
js/data/ue_import/*.csv    仓库内备份
UE Saved/Import/*.csv      需设环境变量 UE_IMPORT_DIR(2026-09-01 起,原为硬编码绝对路径)
        ↓  UE 编辑器导入
DT_Skills / DT_Relics      UE DataTable 资产
```

**断点一:CSV 表头没有 `Kind` 列 —— ✅ web 侧已修(2026-09-01),UE 侧待做**

原状况:web 侧每个技能都有 `kind:'atk'|'aoe'|'heal'`,导出时丢了,所以 UE 的 `IsAoeSkill` 只能**硬编码**比较 `"sweep"/"quake"/"rockslide"/"cleave"` 四个字符串,加第 5 个 AOE 技能要改蓝图。

**已完成(不需要编辑器的那一半)**:
- `export_ue_csv.js` 的 `skillHeader` 加了 `Kind` 列,值**从 `js/data/skills.js` 现查**(`kindOf()`),不在 `combat_tables.js` 里再抄一份副本;查不到就抛错,不静默填默认值。
- 顺带修掉一个**潜在数据丢失 bug**:`sweep/quake/rockslide/cleave` 这 4 行此前只手工加在 CSV 里、**没有加进 `NUMERIC_SKILLS`**——也就是说**任何人跑一次 `node js/data/export_ue_csv.js`,这 4 个 AOE 技能就会被静默抹掉**。已补进表,生成器重新成为唯一来源。
- 已重新生成 CSV 并逐字段核对:35 行一条不少,除新增 `Kind` 列外没有任何实质值变化。

**⏳ UE 侧待做(需要编辑器)**:
1. C++ `FSkillRow`(`CombatTables.h`)加 `Kind` 字段(`FName` 或 `FString`,列名必须与 CSV 表头 `Kind` 完全一致,否则导入时该列被静默忽略)。
2. 在 UE 编辑器里**重新导入** `DT_Skills`(CSV 和 DataTable 不会自动同步,见断点二)。
3. `IsAoeSkill(RowName)` 从硬编码四个字符串比较,改成 `GetDataTableRow → BreakSkillRow → Kind == "aoe"`。
4. 改完之后加第 5 个 AOE 技能就只需要:在 `skills.js` 加一条(带 `kind:'aoe'`)→ 在 `combat_tables.js` 加一条 → 跑导出 → UE 里重新导入。**不用再动蓝图。**

**断点二:CSV 与 DataTable 不自动同步** —— 历史上有过直接用 `DataTableTools.add_rows` 写 DataTable、只把 CSV 当备份的做法(`UE蓝图状态.md:42` 明确写着"两者不会自动同步")。
→ **2026-09-01 起确立单向流**:`skills.js`(kind)+ `combat_tables.js`(数值)→ `export_ue_csv.js` → CSV → UE 编辑器导入 → DataTable。**禁止再直接写 DataTable**,否则下次跑导出就会被覆盖。导出脚本结尾已经会打印"记得在 UE 编辑器里重新导入"的提醒。

**顺带修掉的第三个问题:导出脚本的硬编码绝对路径**
原来写死 `C:/Users/AI_Work/Documents/Unreal Projects/MyProject 5.8/Saved/Import`,换机器/改工程名即失效,且失败模式是 `mkdirSync(recursive)` 静默把错路径建出来、文件写进 UE 永远读不到的空目录。现在改成环境变量 `UE_IMPORT_DIR`:不设就只写仓库副本并提示用法;设了但目录不存在**直接报错退出**(不自动创建)。

---

## 五、战略建议:先补"支撑 TPS 直控的规则",而不是按 web 顺序补

UE 版已经从回合制点击改成了 **TPS 直控 + ARPG 瞄准**。这个改动让部分 web 机制**变得更重要**,也让另一部分**失去意义**:

**变得更重要(建议优先)**
- **地形效果**:玩家现在亲自走位,走位没有回报是最直接的浪费
- **夹击**:直控下"绕背/绕侧"是玩家的本能操作,应该有回报
- **暴击 / 二段**:ARPG 手感需要伤害数字有波动,全是同一个数会很平

**失去意义或需要重新设计(建议缓做)**
- **ZOC**:"脱离邻格"在实时移动下语义不明,需要重新设计而非移植
- **蓄力 / 换位**:没有对应的输入位置
- **击退**:在 3D 直控里做位移会牵扯碰撞/动画,成本远高于 2D 网格

> 换句话说:**不要把 web 的机制清单当成 UE 的待办清单按顺序抄。**TPS 直控是一次玩法形态的改变,该补的是"让直控有意义"的那部分规则。

---

## 六、维护约定

- **改 UE 战斗相关功能前**:先查本表对应行,确认这次改动会不会让某一行的状态变化。
- **改完之后**:回来更新该行状态,和 `UE蓝图状态.md` 同步更新(这是 `CLAUDE.md` 已有的硬性要求)。
- **发现新的不一致**:加进第一节,不要只在 `UE节点备忘录.md` 里记一笔——那份文档是"怎么做",这份是"该不该做"。
- **本表的信息来源**:2026-09-01 的一次全项目评估,数据来自 `UE蓝图状态.md` / `UE节点备忘录.md` 的自述记录 + `js/` 源码 + `DT_Skills.csv` 实际内容。
  ⚠️ **UE 侧未经编辑器直接复核**(当次 MCP 连接失败)。第一节那几条在动手改之前,建议先用 MCP 复核一次真实图结构——这也是 harness 规范本身的要求。
